# نقشه راه طراحی محصول شمش

این سند ترتیب مراحل، خروجی‌ها و معیار عبور را مشخص می‌کند. جزئیات هر مرحله در `README.md` همان پوشه قرار دارد.

| مرحله | هدف | خروجی‌های اصلی | معیار عبور | وضعیت |
|---|---|---|---|---|
| ۱. تعریف محصول | روشن کردن اینکه چه محصولی، برای چه کسی و چرا می‌سازیم | مسئله، چشم‌انداز، مخاطبان، ارزش، اهداف، محدوده | تناقض راهبردی جدی وجود نداشته باشد و فرض‌ها ثبت شده باشند | عبور مشروط تأیید شد — Sufficient to proceed |
| ۲. تحقیق هدفمند و ارزیابی | ساخت ورودی کافی برای طراحی Greenfield بدون وابستگی به محصول یا داده تاریخی قبلی | فرض‌های کاربر، سنتز چهار رقیب، ریسک‌های Legacy و شکاف قوانین | فرض‌ها و شواهد از هم جدا، شکاف‌ها ثبت و ورودی مدل محصول کافی باشد | عبور مشروط تأیید شد — Sufficient to proceed |
| ۳. مدل محصول | تعریف اجزای محصول و قوانین آن | نقشه قابلیت‌ها، اولویت‌ها، نقش‌ها، قوانین، مدل دامنه | قابلیت‌ها و وابستگی‌ها بدون تناقض قابل توضیح باشند | عبور مشروط تأیید شد — Sufficient to proceed |
| ۴. معماری تجربه | ساخت ساختار کلان تجربه | سفرها، IA، ناوبری، مدل محتوا | کارهای اصلی قابل یافتن و مسیرها منسجم باشند | عبور مشروط تأیید شد — Sufficient to proceed |
| ۵. فلوهای کاربر | طراحی رفتار کامل قابلیت‌ها | فلوها، State Matrix، کپی و مدیریت خطا | مسیر موفق، خطا، انتظار، لغو و بازیابی پوشش داده شوند | فعال — فلوهای Release اول ساخته شده؛ Gate Check نهایی در انتظار اقساط و چند تصمیم حقوقی/عددی |
| ۶. وایرفریم و تست | اعتبارسنجی ساختار پیش از UI نهایی | وایرفریم، پروتوتایپ، برنامه تست، یافته‌ها | مسائل بحرانی تست حل یا آگاهانه پذیرفته شوند | شروع نشده |
| ۷. سیستم طراحی | ساخت زبان بصری و رفتاری مشترک | اصول، Foundation، کامپوننت‌ها، الگوها، دسترس‌پذیری | کامپوننت‌ها حالت‌های واقعی و کیفیت قابل تکرار داشته باشند | شروع نشده |
| ۸. طراحی نهایی و تحویل | تکمیل تمام صفحات و آماده‌سازی اجرا | UI نهایی، پوشش صفحات، QA، مشخصات تحویل | پوشش فلو/حالت کامل و ابهام اجرایی بحرانی صفر باشد | شروع نشده |
| ۹. سنجش و تکرار | بررسی اثر واقعی طراحی | برنامه اندازه‌گیری، آزمایش‌ها، مرور پس از انتشار | داده جمع‌آوری و تصمیم بعدی مستند شده باشد | شروع نشده |

## قواعد وابستگی

- تحقیق کاربر و تکمیل قواعد رسمی می‌تواند هم‌زمان ادامه یابد؛ مدل محصول با فرض‌های برچسب‌خورده آغاز می‌شود و پیش از نهایی‌شدن فلوها اعتبارسنجی می‌شود.
- محصول فعلی مرجع طراحی هدف نیست؛ فقط ریسک‌ها، تناقض‌ها و درس‌های واضح آن حفظ می‌شوند.
- نبود داده تاریخی معتبر مانع Gate 2 نیست؛ Baseline سنجش پس از ابزارگذاری، پایلوت یا انتشار ساخته می‌شود.
- طراحی بصری آزمایشی مجاز است، ولی پیش از تثبیت فلوها منبع حقیقت محصول نیست.
- سؤال بیرونی مانع کل پروژه نمی‌شود؛ با فرض طراحی برچسب‌خورده جلو می‌رویم.
- شواهد تازه می‌توانند یک Gate قبلی را باز کنند؛ تغییر باید در Decision Log ثبت شود.
