# لاگ تکرارهای طراحی

| نسخه | تاریخ | مسئله | تغییر | شاهد/یافته | اثر مورد انتظار | نتیجه بازآزمایی |
|---|---|---|---|---|---|---|
| V-115 | 17 Aug 2026 | Tileهای Hub خدمات مستطیلی بودند، قاب خاکستری Background/Border دور آیکن‌ها وزن بصری اضافه داشت و دو ورودی صدور/دریافت هدیه سطح اول را شلوغ می‌کردند | دو ورودی هدیه به یک Tile «هدیه» با آیکن عمومی و مقصد `gift-hub` ادغام شدند؛ Tileها با `aspect-ratio:1` مربع و قاب آیکن فقط در Hub حذف شد. گرید ۹تایی ۳×۳ است و با حضور Tile دهم تا دوازدهم از طریق `:has()` به ۴×۳ متراکم تبدیل می‌شود؛ دو Grid دوگزینه‌ای دیگر تغییر نکردند | درخواست مستقیم مالک محصول؛ D-184؛ قواعد `ui-ux-pro-max` برای سلسله‌مراتب، SVG consistency، Touch target و Responsive overflow | سطح اول خدمات خلوت‌تر و منظم‌تر می‌شود، صدور/دریافت در سطح بعدی قابل انتخاب می‌مانند و توسعه تا ۱۲ خدمت بدون Scroll یا ردیف چهارم ممکن است | JavaScript Parse و Playwright در ۳۲۰×۵۶۸، ۳۷۵×۶۶۷ و ۳۹۰×۸۲۰ پاس شد: ۹ Tile مربع ۹۱–۱۱۴px، آیکن شفاف/Border صفر، مسیر هدیه به دو انتخاب، و ۱۲ Tile آزمایشی مربع ۶۶–۸۴px در چهار ستون با Scroll X/Y صفر و بدون بریدگی محتوا؛ Console بدون Page error بود |
| V-114 | 17 Aug 2026 | Card دارایی کل با زمینه ۸۸٪ Primary هنوز نسبت به Cardهای سفید فلز بیش از حد Bold و شبیه Hero مستقل بود | فقط Variant خانه به Surface روشن با ته‌رنگ ۶٪ Primary، Border با تأکید ۲۲٪ Primary، متن/آیکن تیره و سایه `0 4px 12px rgba(27,35,45,.07)` تغییر کرد؛ ابعاد Card، تایپوگرافی، Privacy، Rail و CTA دست‌نخورده ماندند | درخواست مستقیم مالک محصول؛ D-183؛ قواعد `ui-ux-pro-max` برای Visual hierarchy، Contrast و Surface separation | Card دارایی کل همچنان در نگاه اول قابل تشخیص است، اما به‌جای تسلط بر صفحه فقط یک پله از Cardهای فلز متمایز می‌شود | QA مرورگری ۳۲۰/۳۷۵/۳۹۰/Landscape: Cardهای ۲۰۴/۲۵۹/۲۷۴/۲۷۲×۱۴۸px، Peek برابر ۷۴px داخل Rail (۹۰px تا لبه Viewport همراه Gutter)، CTA تمام‌عرض ۴۴px، Body overflow صفر، Value و Eye داخل Card و Console بدون Error/Warning؛ کنتراست متن اصلی/ثانویه ۱۵٫۷۱:۱ و ۵٫۰۲:۱ پاس شد |
| V-113 | 17 Aug 2026 | Hub خدمات با گرید دو‌ستونه و Tileهای ۱۱۲px برای ۱۰ خدمت Scroll می‌خورد، عنوان «خدمات» را زیر Header تکرار می‌کرد و افزودن خدمت تازه ارتفاع صفحه را سریع افزایش می‌داد | Variant مستقل `service-hub-grid` سه‌ستونه ساخته شد؛ Tileها به حداقل ۷۲px، آیکن ۳۴px و متن مرکزچین رسیدند، Gap هشت‌پیکسلی و Focus/Active حفظ شد و عنوان تکراری حذف شد. آخرین Tile در تعداد `۳n+۱` مرکزچین و با ورود Tile بعدی خودکار آزاد می‌شود؛ Gridهای دوگزینه‌ای دیگر تغییر نکردند | درخواست مستقیم مالک محصول؛ D-182؛ نتایج `ui-ux-pro-max` برای Touch target، Touch spacing و Responsive overflow | ۱۰ خدمت فعلی بدون Scroll و با سلسله‌مراتب متراکم‌تر دیده می‌شوند؛ دو خدمت دیگر بدون ساخت ردیف تازه و بدون تغییر Componentهای دیگر قابل افزودن‌اند | JavaScript Parse و Playwright در ۳۲۰×۵۶۸، ۳۷۵×۶۶۷ و ۳۹۰×۸۲۰ پاس شد: سه ستون، Scroll X/Y صفر، Tile حداقل ۹۱×۷۴px و حالت‌های شبیه‌سازی‌شده ۱۰/۱۱/۱۲ خدمت بدون Scroll؛ Console بدون Page error بود |
| V-112 | 17 Aug 2026 | Card دارایی کل و CTA تمام‌عرض هر دو Filled تیره بودند و به‌صورت دو سطح هم‌وزن دیده می‌شدند؛ Candidate کم‌عرض نیز با ترجیح مالک برای CTA تمام‌عرض سازگار نبود | CTA به عرض کامل Rail، ارتفاع ۴۴px، Surface سفید، Border خنثی و متن Primary برگشت. Card/Main به ۱۴۸/۱۴۶px افزایش یافت، عنوان ۱۵px شد و زمینه Card با ترکیب ۸۸٪ Primary/۱۲٪ Surface و سایه سبک‌تر آرام‌تر شد | درخواست و تأیید مستقیم مالک محصول؛ D-181؛ قواعد `ui-ux-pro-max` برای Visual hierarchy، Primary-action differentiation، Contrast و Touch target | Card دارایی همچنان Anchor اصلی خانه می‌ماند، CTA قابل‌کشف و تمام‌عرض است اما دیگر امتداد بصری Card به نظر نمی‌رسد | QA مرورگری ۳۲۰/۳۷۵/۳۹۰/Landscape: Cardهای ۲۰۴/۲۵۹/۲۷۴/۲۷۲×۱۴۸px، CTA تمام‌عرض ۲۸۸/۳۴۳/۳۵۸/۳۵۶×۴۴px، Value و Eye داخل Card، Body overflow صفر و Console بدون Error/Warning؛ JavaScript Parse و `git diff --check` پاس شدند |
| V-111 | 17 Aug 2026 | Candidate قبلی Home هنوز G5 را کامل اجرا نمی‌کرد: پس‌زمینه عمومی خاکستری بود، چند عدد داخل Cardهای فلز با وزن بصری نزدیک پخش می‌شدند و صفحه ترکیب دارایی Hero تیره و Rowهای Cardشده داشت | فقط در کپی پایلوت، Home به Canvas سفید و یک Hero «ارزش کل فلزها» تبدیل شد؛ Card هر فلز فقط ارزش فعلی را Primary و وزن/P&L را Meta می‌کند؛ کیف و Shortcut تخت و Campaign کم‌اولویت شدند. صفحه ترکیب دارایی Summary تایپوگرافیک، Chart module مستقل و Rowهای ۶۴px بدون Wrapper Card گرفت. Geometry فعال D-180، ترتیب، Privacy مستقل و رفتارهای شمش حفظ شدند | تأیید اجرای Pilot پس از D-177؛ Visual Direction Contract G5؛ D-180؛ ممیزی `ui-ux-pro-max` برای Numeric hierarchy، Card taxonomy، Touch target و Responsive overflow | تمرکز عددی Home به یک Anchor برمی‌گردد، اطلاعات فلزها قابل‌مقایسه ولی کم‌رقابت می‌مانند و Assets به فضای سفید/مینیمال/مرتب نزدیک می‌شود بدون کپی هویت بایننس | JavaScript Parse و `git diff --check` پاس؛ QA ۳۲۰/۳۹۰px Cardهای ۲۰۴/۲۷۴px و Peek برابر ۷۴px، Overflow صفر، Touch target حداقل ۴۴px، ارتفاع Mask ثابت ۱۰۰/۱۴۲/۷۲px، نرخ ناقص/توقف معامله، Rowهای ۶۴px، Dark با Surfaceهای ۱۰/۲۳/۳۸، Reduced Motion بدون Auto-cycle و Console بدون Error/Warning را ثبت کرد. H05 Dynamic Type 200% و Review بصری مالک محصول بازند؛ وایرفریم مرجع و Figma تغییر نکردند |
| V-110 | 17 Aug 2026 | نسبت D-178 هنوز به‌اندازه مدنظر مالک عمودی نبود: Card در ۳۹۰px عرض ۲۹۴ و ارتفاع ۱۲۶px داشت و فقط ۵۴px از Card بعدی دیده می‌شد | Flex basis از `calc(100% - 64px)` به `calc(100% - 84px)`، حداقل ارتفاع Card/Main به ۱۳۲/۱۳۰px و Padding عمودی Main به ۱۴px تغییر کرد. حالت `is-xlong` برای حفظ اعداد بسیار طولانی در عرض باریک‌تر از ۸۰٪ به ۷۲٪ کاهش یافت | درخواست مستقیم مالک محصول؛ D-180؛ قواعد `ui-ux-pro-max` برای Mobile-first، Numeric fit و پرهیز از Clipping | Card باریک‌تر و بلندتر دیده می‌شود، Affordance ادامه Rail قوی‌تر است و شرط عدم بیرون‌زدگی عددهای بزرگ حفظ می‌شود | QA مرورگری ۳۲۰/۳۷۵/۳۹۰/Landscape: Card به‌ترتیب ۲۰۴/۲۵۹/۲۷۴/۲۷۲px عرض و در همه ۱۳۲px ارتفاع، Peek برابر ۷۴px، Value و Toggle داخل Card، Body overflow صفر و Console بدون Error/Warning بود؛ Review بصری مالک محصول باز است |
| V-109 | 17 Aug 2026 | Header مشترک اعلان/پروفایل در Entry معامله فضای عمودی تکراری می‌گرفت و Divider بالای کیبورد، CTA «خرید/فروش فلز» را از صفحه‌کلید بیش از حد جدا می‌کرد | Header فقط در سناریوهای Entry `trade-*` مخفی شد، Asset Tab به ابتدای محتوا آمد، Divider بالای کیبورد حذف و عنوان معنایی `sr-only` اضافه شد. Preview/Processing و سایر Root/زیرصفحه‌ها قرارداد Header قبلی را حفظ کردند | درخواست مستقیم مالک محصول؛ D-179؛ قواعد `ui-ux-pro-max` برای سلسله‌مراتب ناوبری، فضای مؤثر موبایل و حفظ عنوان دسترس‌پذیر | صفحه معامله متراکم‌تر و یکپارچه‌تر می‌شود، بدون اینکه Bottom Navigation یا Back زیرصفحه‌ها از دست برود | JavaScript Parse و Assertionهای Header/Divider/عنوان پاس شدند؛ QA Playwright در ۳۹۰px نمایش Header خانه=`grid`، معامله=`none`، Divider=`0px`، Asset Tab در `top=0` و بازیابی Header خانه را تأیید کرد؛ Console بدون Page error بود |
| V-108 | 17 Aug 2026 | D-175 فقط عرض کارت‌های دارایی خانه را افزایش داده بود؛ ارتفاع ۹۸px و عدد اصلی ۲۲px باقی مانده و عدد/واحدهای طولانی مستعد اشغال عرض یا بیرون‌زدگی بودند | حداقل ارتفاع Card/Main به ۱۱۶/۱۱۴px افزایش یافت؛ عدد اصلی با `clamp(18px,5vw,20px)` کوچک شد، واحد Card به ۷۲٪ رسید و Renderer مشترک برای عددهای ۱۰+ و ۱۳+ رقمی کلاس فشرده خودکار می‌دهد. معادل تومانی فلز نیز از ۱۳ به ۱۲px کاهش یافت؛ عرض و Peek D-175 حفظ شدند | درخواست مستقیم مالک محصول؛ D-178؛ قواعد `ui-ux-pro-max` برای Number hierarchy، Tabular figures، Dynamic type و Responsive overflow | Card علاوه بر عرض، عمق و فضای عمودی بیشتری می‌گیرد؛ عدد ارزش کل/گرم کم‌عرض‌تر است و رشد مقدار دارایی بدون شکستن Card مدیریت می‌شود | JavaScript Parse پاس؛ QA مرورگری در ۳۲۰/۳۷۵/۳۹۰ و Landscape ارتفاع ۱۲۴–۱۲۶px، قرارگیری کامل عدد ده‌رقمی/واحد، Body overflow صفر و Console بدون Error/Warning را ثبت کرد؛ Review بصری مالک محصول باز است |
| V-107 | 17 Aug 2026 | `LivePriceTicker` در Contextهای فلزمحور و دارای Tab روی Asset منتخب ثابت می‌ماند و فقط صفحه‌های عمومی اسلاید می‌خوردند | شاخه‌های تک‌Asset نمودار، تاریخچه و Featureهای فیزیکی/قیمت/اقساط/هدیه حذف شدند؛ هر Instance قابل‌نمایش اکنون آرایه مشترک `gold/silver/copper` می‌گیرد. استثناهای صفحه‌های حساس و نرخ اصلی بازار/معامله/جزئیات حفظ شدند | درخواست مستقیم مالک محصول؛ D-176؛ راهنمای `apple-design` برای Motion consistency، Spatial continuity و Reduced Motion | کاربر در همه Contextهای دارای Ticker رفتار یکسان و قابل‌پیش‌بینی می‌بیند و وجود Tab فلز حرکت را متوقف نمی‌کند | JavaScript Parse، هشت Context چرخان و شش Context مستثنا تست شدند؛ حفظ rAF/Focus/Reduced Motion و `git diff --check` پاس شد؛ Review بصری مالک محصول باز است |
| V-106 | 17 Aug 2026 | کارت‌های دارایی خانه با عرض `Rail - 104px` کوچک بودند و بخش زیادی از کارت بعدی دیده می‌شد؛ مالک محصول Card غالب‌تر همراه Peek محدودتر می‌خواست | Flex basis کارت‌های `portfolio-rail` به `calc(100% - 64px)` تغییر کرد؛ ارتفاع، محتوای داخلی، Gap ده‌پیکسلی، Scroll-snap و Touch targetها دست‌نخورده ماندند | درخواست مستقیم مالک محصول؛ D-175؛ قواعد `ui-ux-pro-max` برای Mobile-first، حفظ Affordance پیمایش و جلوگیری از سرریز Body | کارت اصلی وزن بصری بیشتری می‌گیرد و لبه کارت بعدی همچنان ادامه Rail را در همان لحظه اول نشان می‌دهد | QA مرورگری در ۳۲۰px: Rail=۲۸۸، Card=۲۲۴، Peek=۵۴؛ در ۳۹۰px: Rail=۳۵۸، Card=۲۹۴، Peek=۵۴. Body overflow در هر دو صفر، Console بدون Error/Warning و `git diff --check` پاس شد؛ Review بصری مالک محصول باز است |
| V-105 | 17 Aug 2026 | Prototype با `hasActiveInstallment=true` شروع می‌شد؛ بنابراین ورود واقعی از خانه/خدمات مستقیماً قرارداد فعال را نشان می‌داد و امکان مشاهده پیوسته ساخت قرارداد از State اولیه وجود نداشت | مقدار اولیه `hasActiveInstallment` به `false` تغییر کرد؛ ورودی عادی به `installment-empty` می‌رود و هر دو مسیر پرداخت کیف/درگاه فقط پس از موفقیت State را فعال می‌کنند. Success، خانه و ورود بعدی به قرارداد فعال متصل باقی ماندند؛ سناریوی `WF-MM-023A` برای Review مستقیم حفظ شد | درخواست مستقیم مالک محصول؛ D-174؛ قواعد `ui-ux-pro-max` برای State continuity و Predictable navigation | چرخه واقعی کاربر از بدون قرارداد تا ساخت قرارداد و سپس مدیریت اقساط در یک Session قابل بررسی است | JavaScript Parse و شش Assertion State اولیه، Entry، فعال‌سازی کیف/درگاه، Success و ورود بعدی پاس شدند؛ `git diff --check` بدون خطا بود و Review بصری مالک محصول باز است |
| V-104 | 17 Aug 2026 | ویدئوی تازه نشان داد اجرای V-102 با Web Animations API در `file://` همچنان فریم میانی نمایش نمی‌دهد و طلا→نقره در یک فریم می‌پرد؛ QA قبلی روی HTTP تفاوت محیط ارائه را پوشش نداده بود | Web Animation حذف شد؛ `requestAnimationFrame` طی ۴۶۰ms با Easing سینوسی مقدار `top` را از ۰ تا ۳۸- پیکسل مستقیماً در هر فریم می‌نویسد. Track دو ردیف `current → next` را تا پایان نگه می‌دارد و سپس Style و DOM را به ردیف بعدی در صفر بازمی‌گرداند | ویدئوی مالک محصول `Recording.mp4` و مقایسه ۳۰fps با مرجع اصلی؛ D-173؛ اصل تطبیق QA با محیط واقعی ارائه | حرکت دیگر به Commit شدن CSS/WAAPI وابسته نیست و تغییر موقعیت فریم‌به‌فریم در همان فایل تک‌فایلی اجرا می‌شود | JavaScript Parse و Assertionهای حذف WAAPI/وجود rAF پاس شدند؛ QA مرورگری دو چرخه طلا→نقره→مس را با ۱۱ نمونه میانی شامل `-0.46/-5.65/-17.19/-29.53/-37.15px` و پایان تک‌ردیفی `top:0` ثبت کرد؛ Console بدون Error/Warning و `git diff --check` پاس شد؛ Review تصویری نهایی مالک محصول روی `file://` باز است |
| V-103 | 17 Aug 2026 | در Contextهای داخل Card، عنوان‌های «فلز» و «فلز هدیه» بالای Asset Tab فضای عمودی اضافه می‌گرفتند و Tab را برخلاف معامله/اقساط از ابتدای محتوا دور می‌کردند | عنوان بصری و Wrapper فیلد از هشدار/سفارش قیمت و صدور هدیه حذف شد؛ `metalTabs()` مستقیماً اولین فرزند Card شد تا فاصله بالا از Padding استاندارد ۱۴px و فاصله تا کنترل بعدی از Margin مشترک ۱۴px فیلد تأمین شود؛ `aria-label` خود Tablist حفظ شد | تصویر و درخواست مستقیم مالک محصول؛ ادامه V-101 | تب‌ها بدون تیتر تکراری، با ریتم عمودی یکسان‌تر و فضای مؤثر بیشتر نمایش داده می‌شوند؛ نام هدف انتخاب برای Screen reader باقی می‌ماند | JavaScript Parse، صفر عنوان بصری/Wrapper قدیمی، حفظ پنج Call site و `git diff --check` پاس شدند؛ Claim/Reload تب باز `file://` طبق سیاست امنیتی Browser مسدود شد و Review بصری دستی باز است |
| V-102 | 17 Aug 2026 | با وجود اصلاح جهت در V-099، CSS Transition هم‌زمان با Transform مقصد به Track اضافه می‌شد و مرورگر در اجرای واقعی فریم شروع را Commit نمی‌کرد؛ ویدئوی بازآزمایی تعویض ناگهانی مس→طلا را نشان داد | CSS Transition و Timer ثابت حذف و Web Animations API با Keyframeهای `translateY(0) → translateY(-38px)`، مدت ۴۲۰ms و Easing تدریجی `cubic-bezier(.4,0,.2,1)` اضافه شد؛ ردیف `current → next` تا رویداد واقعی `finish` در DOM می‌ماند و سپس بدون Fill ماندگار در موقعیت صفر تثبیت می‌شود | ویدئوی مالک محصول `Recording 2026-08-17 115220.mp4` در مقایسه با مرجع اصلی؛ D-172؛ اصل Frame-level smoothness و compositor-only Transform در `apple-design` | حداقل چند فریم میانی قابل مشاهده ساخته می‌شود و نام فلز/قیمت بدون پرش روی یک مسیر فضایی پیوسته حرکت می‌کنند | JavaScript Parse و Assertionهای Web Animation پاس شدند؛ QA مرورگری دو چرخه طلا→نقره→مس را با هفت فریم میانی متمایز از `-2.86px` تا `-37.95px`، دو ردیف هم‌زمان و پایان تک‌ردیفی در Transform صفر ثبت کرد؛ Console بدون Error/Warning و `git diff --check` محدود به فایل‌های اصلاح بدون خطا بود |
| V-101 | 17 Aug 2026 | تب انتخاب فلز در معامله و خرید قسطی بدون قرارداد ظاهر یکسان داشت اما Renderer مستقل بود؛ دریافت فیزیکی، هشدار/سفارش قیمت و هدیه نیز پیاده‌سازی‌های جدا داشتند و تغییر بعدی می‌توانست آن‌ها را ناهماهنگ کند | Renderer مرکزی `metalTabs()` با ورودی فلز منتخب، Action، Label، `aria-controls` و Class زمینه‌ای ساخته شد؛ پنج Context به آن متصل، CSS روی `metal-tabs / metal-tab` متمرکز و پیمایش Arrow/Home/End از معامله به Component عمومی منتقل شد؛ Selectorهای نمودار و تاریخچه چون Tab فلز نیستند مستقل ماندند | درخواست مستقیم مالک محصول؛ ادامه قرارداد Asset Tab در D-152 و الگوی یکپارچه‌سازی Component در D-156 | هر تغییر بعدی ظاهر، State و دسترس‌پذیری Asset Tab از یک محل به همه Contextهای Tab فلز منتشر می‌شود و تفاوت ناخواسته حذف می‌شود | JavaScript Parse، وجود دقیق یک Renderer، پنج Call site، یک Style source، صفر نام Legacy و Handler کیبورد عمومی پاس شدند؛ Browser خودکار `file://` را طبق سیاست امنیتی باز نکرد و QA بصری/لمسی مالک محصول باز است |
| V-100 | 17 Aug 2026 | Tabهای وضعیت قرارداد با تاریخچه پرداخت قاطی بودند؛ انتخاب تسویه/فسخ Tabها را حذف می‌کرد، خلاصه و قسط بعدی جداسازی کافی نداشتند و فهرست شش قسط/جزئیات قرارداد در مسیر اصلی نبود | مقصد ثابت «قسط‌های من» با Tabهای درجا ساخته شد؛ جاری به خلاصه قرارداد، قسط بعدی، برنامه شش‌قسطی و جزئیات تفکیک شد؛ پرداخت‌شده‌ها به رسید، قسط قابل پرداخت به Payment review و آینده به حالت اطلاعاتی متصل شدند؛ Empty state تسویه/فسخ داخل Tab ماند و صفحه پرداخت/نتیجه بازچینی شد | درخواست مستقیم مالک محصول؛ D-171؛ شواهد `figma-my-installments-payment-evidence-2026-08-16.md`؛ قواعد `ui-ux-pro-max` برای Persistent navigation، Progress، Touch target، Content priority و Back behavior | کاربر بدون گم‌کردن Context بین وضعیت قراردادها جابه‌جا می‌شود، می‌فهمد کدام قسط پرداخت/قابل‌پرداخت/آینده است و از هر سطح مسیر روشن به جزئیات، رسید یا پرداخت دارد | JavaScript Parse، Render harness برای جاری/تسویه/فسخ/Detail/Payment، بررسی اتصال Receipt/Pay/Result، Touch target حداقل ۴۴px در کنترل‌های تازه و `git diff --check` پاس شدند؛ QA بصری مالک محصول و Stateهای مالی/حقوقی OQ-005/OQ-045/OQ-054 بازند |
| V-099 | 17 Aug 2026 | موشن عمودی `LivePriceTicker` جهت معکوس ویدئوی مرجع داشت و لازم بود روشن باشد نام فلز و قیمت جداگانه حرکت نمی‌کنند | ترتیب Track به `current → next` و مقصد Transform به `-38px` تغییر کرد؛ نام فلز، قیمت و واحد در همان `livePriceRow` واحد باقی ماندند تا ردیف فعلی به بالا خارج و ردیف بعدی از پایین وارد شود. زمان ۳۶۰ms، Easing، Tap، چرخه ۳٫۸ثانیه‌ای و Reduced Motion تغییر نکردند | درخواست مستقیم مالک محصول و تحلیل فریم‌به‌فریم ویدئوی `Recording 2026-08-15 172903.mp4`؛ D-170 | حرکت با مرجع فضایی سازگار و اطلاعات هر فلز در تمام فریم‌های انتقال یکپارچه می‌ماند | JavaScript Parse و Assertionهای ترتیب DOM/Transform/ردیف واحد پاس شدند؛ QA مرورگری صفحه خانه تعویض طلا→نقره و Console بدون Error/Warning را تأیید کرد و `git diff --check` محدود به فایل‌های همین اصلاح بدون خطا بود |
| V-098 | 17 Aug 2026 | شرط حداقل معامله فقط وقتی `calc.input > 0` بود اجرا می‌شد؛ بنابراین واردکردن صریح `۰ گرم` با حالت ورودی خالی یکی تلقی و بدون پیام خطا نمایش داده می‌شد | تشخیص «ورودی صریح» از مقدار عددی جدا شد؛ ورودی خالی خنثی می‌ماند اما هر مقدار واردشده با وزن کمتر از ۰٫۰۰۱ گرم، از جمله صفر، خطای «حداقل مقدار معامله ۱ سوت است» می‌دهد | بازخورد مستقیم مالک محصول؛ اجرای دقیق D-169 / BR-113 | کاربر بعد از واردکردن صفر بازخورد روشن می‌گیرد، بدون اینکه صفحه در ورود اولیه با خطا باز شود | JavaScript Parse و سه تست ورودی خالی، `۰ گرم` و `۰٫۰۰۱ گرم` پاس شدند؛ `git diff --check` بدون خطا بود |
| V-097 | 17 Aug 2026 | Formatter تومان از `\B` استفاده می‌کرد؛ چون ارقام فارسی در JavaScript عضو `\w` نیستند، عدد دقیقاً سه‌رقمی مانند `۲۲۲` به‌شکل `٬۲۲۲` نمایش داده می‌شد | گروه‌بندی به Regex صریح ارقام فارسی تغییر کرد که جداکننده را فقط بعد از رقمی می‌گذارد که در سمت راستش یک یا چند گروه کامل سه‌رقمی باقی مانده باشد | تصویر و بازخورد مستقیم مالک محصول؛ اصلاح اجرای D-169 | عدد سه‌رقمی بدون جداکننده و اعداد چهاررقمی به بالا با گروه‌بندی صحیح از سمت راست نمایش داده می‌شوند | JavaScript Parse و شش تست `۲`، `۲۲۲`، `۲٬۲۲۲`، `۲۲۲٬۲۲۲`، `۲٬۲۲۲٬۲۲۲` و `۵۰٬۰۰۰٬۰۰۰٬۰۰۰` پاس شدند؛ `git diff --check` نیز بدون خطا بود |
| V-096 | 17 Aug 2026 | جداکننده مبلغ تومان به‌علت جهت LTR در سمت نادرست عدد دیده می‌شد؛ Entry معامله کسری کیف را Error می‌کرد و حدود رسمی معامله/تخصیص پرداخت در Preview پوشش نداشت | جهت عدد با RTL isolate اصلاح شد؛ حداقل ۱ سوت، سقف ۵۰ میلیارد تومان و سقف ۱۰٬۰۰۰ گرم جای Error موجودی را گرفتند. در Preview خرید، Card کیف هم‌الگوی اقساط با Switch اضافه شد و پرداخت کامل از کیف یا پرداخت ترکیبی کیف+درگاه را با Breakdown نشان می‌دهد؛ فروش Card کیف ندارد. سقف ۵۰ میلیاردی ورودی تومان را به ۱۱ رقم افزایش داد و V-092 را در این بخش جایگزین کرد | درخواست مستقیم و تصویر مالک محصول؛ D-169؛ BR-113/BR-114؛ قواعد `ui-ux-pro-max` برای Number formatting، Error proximity، Progressive disclosure و State clarity | عدد تومان درست خوانده می‌شود، Entry فقط خطای دامنه مقدار می‌دهد و تصمیم منبع پرداخت در لحظه مناسب با مبلغ‌های شفاف انجام می‌شود | JavaScript Parse، ۹ تست مرزی حداقل/حداکثر، ۱۱ رقم و پرداخت کامل/ترکیبی، Markerهای UI و `git diff --check` پاس شدند؛ Review بصری تب باز و تأیید فنی Split payment باقی است |
| V-095 | 17 Aug 2026 | نتیجه موفق اقساط Badge و توضیح تکراری داشت، اعتبار خرید را نشان نمی‌داد، Dividerهای ابتدا/انتهای جدول اضافه بودند و اقدام اول به خدمات می‌رفت و Sticky بود | Badge و توضیح حذف شدند؛ «مقدار اعتبار» از `principal` بالای مقدار فلز آمد؛ خط بالای جدول و خط زیر کد پیگیری حذف شدند؛ اقدام‌ها به «مشاهده اقساط / صفحه اصلی» تغییر و با `inline-action` از رفتار Sticky خارج شدند | درخواست مستقیم مالک محصول؛ D-168؛ قواعد `ui-ux-pro-max` برای Receipt hierarchy، حذف تکرار، Touch target و Action clarity | رسید سریع‌تر خوانده می‌شود، اعتبار و دارایی هم‌زمان روشن‌اند و اقدام‌ها فقط پس از اسکرول دیده می‌شوند | JavaScript Parse، هشت Assertion متن/ساختار/ناوبری/Divider و `git diff --check` پاس شدند؛ Review بصری مالک محصول باز است |
| V-094 | 17 Aug 2026 | CTA صفحه نهایی فقط مسیر پرداخت را می‌گفت و کاربر برای دیدن مبلغ باید دوباره به Breakdown بالا نگاه می‌کرد | مبلغ `feesTotal` به متن CTA افزوده شد و دکمه در دو State عبارت «پرداخت [مبلغ] تومان از کیف پول/درگاه بانکی» را نشان می‌دهد | درخواست مستقیم مالک محصول؛ D-167؛ قواعد `ui-ux-pro-max` برای Action clarity و Number formatting | مبلغ و منبع پرداخت در نقطه تعهد نهایی هم‌زمان دیده می‌شوند | JavaScript Parse، Assertion اتصال CTA به `feesTotal` و هر دو منبع پرداخت و `git diff --check` پاس شدند؛ Review بصری مالک محصول باز است |
| V-093 | 17 Aug 2026 | صفحه پرداخت هزینه شروع با خط اضافی بالای نخستین ردیف آغاز می‌شد، «مقدار تقریبی» و پس‌زمینه مبلغ قابل پرداخت شلوغی بصری داشتند و Card کیف به Breakdown چسبیده و سلسله‌مراتب داخلی ضعیفی داشت | خط بالای نخستین ردیف و پس‌زمینه مبلغ قابل پرداخت حذف شد؛ Label به «مقدار [فلز]» کوتاه و نرخ Candidate `(۰٫۵٪)` کنار کارمزد افزوده شد؛ Card کیف با فاصله بیشتر، Header «پرداخت از کیف پول + Switch» و سطح مستقل «موجودی کیف پول» بازچینی شد | درخواست مستقیم مالک محصول؛ D-166؛ قواعد `ui-ux-pro-max` برای Section spacing، Label-control proximity، Touch target و سلسله‌مراتب اطلاعات | Breakdown سریع‌تر اسکن می‌شود و روشن‌کردن پرداخت کیف و مشاهده موجودی به دو سطح روشن اما مرتبط تبدیل می‌شوند | JavaScript Parse، هفت Assertion متن/ساختار/استایل و `git diff --check` پاس شدند؛ Review بصری در تب باز مالک محصول باقی است |
| V-092 | 17 Aug 2026 | عدد اصلی معامله با طول ورودی از ۴۸ تا ۳۰px کوچک می‌شد، سقف ۱۲ رقم داشت و مقدار تومان هنگام ورود بدون قرارداد صریح جداکننده سه‌رقمی بود؛ رفتار گرم نیز از نظر طول و اندازه یکسان نبود | سقف ورودی به ۱۰ رقم عددی برای هر دو واحد کاهش یافت؛ اندازه عدد اصلی در تمام طول‌ها و هر دو واحد روی ۳۰px ثابت شد؛ Formatter مرکزی تومان را با جداکننده `٬` سه‌رقمی نمایش می‌دهد و گرم را بدون جداکننده هزارگان، با دقت مجاز هر فلز نگه می‌دارد. Keypad، Keyboard و تبدیل واحد از همین Formatter استفاده می‌کنند | درخواست مستقیم مالک محصول؛ قواعد `ui-ux-pro-max` برای Number formatting، Tabular figures و Mobile input | عدد در طول ورود پرش اندازه ندارد، مبلغ تومان سریع‌تر خوانده می‌شود و قرارداد طول/نمایش تومان و گرم قابل‌پیش‌بینی است | JavaScript Parse، هفت تست ورودی برای ۱/۱۰/۱۱ رقم، گروه‌بندی تومان، گرم بدون گروه و دقت طلا/نقره/مس و `git diff --check` پاس شدند؛ Review بصری در تب باز مالک محصول باقی است |
| V-091 | 17 Aug 2026 | مقدار فلز زیر Slider یک ردیف تکراری با Label «مقدار تقریبی» بود؛ Consent لینک دوم داشت؛ صفحه پرداخت هزینه شروع مبلغ خرید را تکرار می‌کرد و Card کیف Divider و راهنمای اضافه داشت؛ CTA منبع پرداخت فعال را نام نمی‌برد | مقدار زنده فلز به زیر مبلغ خرید منتقل و ردیف پایین حذف شد؛ عبارت «شرایط و مقررات» داخل جمله پذیرش به تنها Trigger Sheet تبدیل شد؛ ردیف مبلغ خرید حذف و «جمع هزینه شروع» به «مبلغ قابل پرداخت» تغییر کرد؛ Card کیف به عنوان «پرداخت از کیف پول» + موجودی + Switch بدون Divider/Helper کاهش یافت و CTA با Switch بین کیف و درگاه تغییر می‌کند | درخواست مستقیم مالک محصول؛ D-165؛ قواعد `ui-ux-pro-max` برای Progressive disclosure، یک Trigger معنایی، State clarity، Touch target و Label صریح CTA | مقدار تومان و فلز در یک خوشه خوانده می‌شوند، Consent تکرار ندارد و کاربر پیش از Tap می‌داند پرداخت از کیف یا درگاه انجام می‌شود | JavaScript Parse، ۱۱ Assertion متن/ساختار/Sync/Switch و `git diff --check` پاس شدند. Reload خودکار تب باز `file://` طبق سیاست امنیتی مرورگر ممکن نبود؛ Review بصری مالک محصول باز است |
| V-090 | 17 Aug 2026 | عنوان‌های چهار میانبر خدمات صفحه خانه، به‌ویژه «خرید قسطی»، «دریافت فیزیکی» و «سفارش قیمت»، در عرض موبایل امکان شکستن به دو خط داشتند | یک Rule محدود به `home-service-strip` اضافه شد که عنوان هر چهار میانبر را `nowrap` و اندازه متن را با `clamp(9px, 2.8vw, 11px)` تطبیقی می‌کند؛ Hub خدمات و ارتفاع/سطح لمس دکمه‌ها تغییر نکرد | درخواست مستقیم مالک محصول؛ قواعد `ui-ux-pro-max` برای Mobile-first، Touch target و خوانایی Label | چهار عنوان در یک ردیف هم‌ارتفاع و تک‌خطی دیده می‌شوند و ریتم بصری میانبرهای خانه ثابت می‌ماند | JavaScript Parse، Scope یکتای Rule و `git diff --check` پاس شد؛ مرورگر خودکار اجازه بازکردن `file://` را نداد و QA بصری نهایی در تب باز مالک محصول انجام می‌شود |
| V-089 | 16 Aug 2026 | Slider مبلغ `WF-MM-023B` به‌علت Render کامل در هر `input` پس از یک Step تماس Pointer را از دست می‌داد؛ مقدار با تغییر فلز/بازگشت حفظ می‌شد؛ Preview Lead و ویرایش اضافی و CTA ثابت داشت؛ لینک قوانین از Checkbox جدا و پرداخت هزینه شروع با دو Card روش پرداخت سنگین بود | Slider به Sync زنده همان Outputها بدون بازسازی DOM تغییر کرد و Reset تغییر فلز/ورود مجدد اضافه شد؛ مقدار تقریبی بولدتر شد؛ Lead «فلز · دوره» و توضیح، ویرایش و Sticky CTA بررسی قرارداد حذف و CTA انتهای Scroll شد؛ لینک شرایط داخل گروه Consent رفت؛ شرایط با BR-092..097 و Strongهای معنایی همگام شد؛ عنوان صفحه هزینه شروع «خرید قسطی» و روش پرداخت به Card موجودی + Switch کیف تبدیل شد که در حالت خاموش/کسری موجودی CTA را به درگاه می‌برد | درخواست مستقیم مالک محصول؛ D-164؛ BR-092..097؛ قواعد `ui-ux-pro-max` درباره Gesture feedback، Input latency، Touch target، Progressive disclosure، Disabled semantics و Scroll/fixed coexistence | Drag بدون قطع‌شدن ادامه می‌یابد، مقایسه فلز از مقدار پایه شروع می‌شود، بررسی قرارداد خلوت‌تر و آگاهی حقوقی نزدیک Consent است و تصمیم کیف/درگاه با یک Toggle روشن انجام می‌شود | JavaScript Parse، Assertionهای Sync/Reset/حذف Lead/Consent/عنوان/Switch و `git diff --check` پاس شدند. Browser خودکار `file://` را طبق سیاست امنیتی باز نکرد؛ QA بصری/لمسی مالک محصول و تأیید حقوقی متن همچنان باز است |
| V-088 | 16 Aug 2026 | `WF-MM-023A` فقط یک Card خلاصه داشت و الگوی قابل‌استفاده Figma برای تب وضعیت، پیشرفت قرارداد، قسط بعدی، بازبینی پرداخت، Processing و نتیجه قطعی را پوشش نمی‌داد | مسیر قرارداد فعال از فرم ساخت قرارداد جدید جدا شد؛ صفحه «قسط‌های من» با تب جاری/تسویه‌شده/فسخ‌شده، شناسه و دارایی قفل‌شده، Progress عددی، قسط بعدی و CTA پرداخت ساخته شد. صفحه بازبینی مبلغ/جریمه/سررسید/منبع، Loading اختصاصی و رسید موفق با پیشرفت به‌روزشده و کد پیگیری نیز متصل شدند؛ `WF-MM-023B` و محاسبات ساخت قرارداد دست‌نخورده ماندند | شواهد Legacy ثبت‌شده در `figma-my-installments-payment-evidence-2026-08-16.md`؛ D-089/D-127/D-162؛ OQ-005/OQ-045/OQ-054؛ قواعد `ui-ux-pro-max` برای State clarity، Progress، Feedback، Touch target و یک CTA اصلی | قرارداد فعال و اثر پرداخت از یک مسیر پیوسته و قابل‌پیگیری دیده می‌شود، بدون اینکه عددها و قواعد Legacy به قانون شمش تبدیل شوند | JavaScript Parse و شش Assertion ساختاری پاس شد؛ QA تعاملی ۳۷۵px مسیر ۴/۶ → پرداخت → Processing → رسید ۵/۶ را تأیید کرد، Touch targetهای ۴۴/۴۸px، Landscape بدون سرریز و Console بدون Error بود. Review مالک محصول و تأیید قواعد مالی همچنان باز است |
| V-087 | 16 Aug 2026 | فایل آزمایشی اقساط مسیر مفیدی برای وزن تقریبی، قفل دارایی، پذیرش شرایط و پرداخت هزینه شروع داشت، اما متن حقوقی خام، عبارت‌های طلای هاردکد، کارمزد خرید ۵٪ و Toggle مبهم کیف/درگاه برای انتقال مستقیم به Prototype مرجع مناسب نبودند | فقط تغییرات اقساطی پالایش و منتقل شدند: Slider با گام پنج‌میلیونی و خروجی وزن Asset منتخب، Preview با هشدار قفل دارایی، Checkbox و Sheet خلاصه شرایط چندفلزی، صفحه تفکیک هزینه شروع، انتخاب صریح کیف تومان/درگاه، Loading درگاه و Success فعال‌سازی. متن حقوقی طولانی به خلاصه مبتنی بر D-089 تبدیل و نرخ ۵٪ رد شد؛ کارمزد خرید ۰٫۵٪، هزینه زیرساخت ۲٪ و موجودی کیف فقط داده Candidate محاسباتی‌اند | فایل آزمایشی مالک محصول؛ D-089/D-127/D-144/D-151/D-162؛ OQ-005/OQ-020/OQ-045؛ ممیزی `ui-ux-pro-max` برای Label، Progressive disclosure، Feedback، Touch target، Consent و CTA ثابت | تصمیم خرید پیش از تعهد روشن‌تر می‌شود، وضعیت قفل دارایی و هزینه شروع پنهان نمی‌ماند و فعال‌سازی از فرم تا نتیجه بدون بن‌بست قابل Review است | JavaScript Parse، هفت Marker مسیر، Guard حذف متن/نرخ خام، محاسبات ۱۰/۵۰/۱۰۰ میلیون برای ۶/۱۲ ماه، Touch targetهای ۴۴–۶۴px و `git diff --check` پاس شدند؛ Review بصری مالک محصول و تأیید مالی/حقوقی همچنان باز است |
| V-086 | 16 Aug 2026 | حالت بدون قرارداد اقساط Lead، Tag، عنوان Card و Label فلز تکراری داشت؛ انتخاب فلز با معامله هم‌الگو نبود و مبلغ/قسط فقط ورودی خام و Placeholder داشت | Lead و متن «قسط فعالی ندارید»، Tag «بدون قرارداد فعال»، عنوان «خرید قسطی جدید» و Label فلز حذف شدند؛ Asset Tab مشترک معامله جای Choice شد؛ Slider مبلغ ۱۰–۱۰۰ میلیون با Step یک‌میلیونی، دوره‌های Candidate شش/دوازده‌ماهه و Summary زنده مبلغ هر قسط، سود سالیانه ۲۳٪ و مجموع بازپرداخت اضافه شد. محاسبه با اقساط مساوی و نرخ ماهانه `۲۳٪÷۱۲` انجام می‌شود | درخواست مستقیم مالک محصول؛ Candidate متعارض با دوره ۳ماهه D-089 و ثبت‌شده زیر OQ-005؛ قواعد `ui-ux-pro-max` برای Progressive disclosure، Touch target و عددهای Tabular | صفحه کوتاه‌تر و قابل‌اسکن‌تر می‌شود و کاربر اثر مبلغ/دوره را پیش از Preview می‌بیند | JavaScript Parse، Assertion حذف Copyها، بازه/Step Slider، دو دوره، فرمول ۶/۱۲ماهه، منطق Keyboard Tab و `git diff --check` پاس شد؛ Review بصری و تأیید فرمول رسمی باز است |
| V-085 | 16 Aug 2026 | خط Divider زیر عنوان‌های فلز در Entry معامله باقی مانده بود؛ Hover چهار Shortcut خانه تا لبه‌های کانتینر امتداد داشت و Radius/فاصله مستقل نداشت | Border پایین `trade-asset-tabs` حذف شد؛ `service-strip` به Padding داخلی ۸px و Gap چهارپیکسلی و هر `service-strip-item` به Radius ده‌پیکسلی و Padding متعادل مجهز شد | درخواست مستقیم مالک محصول؛ قواعد `ui-ux-pro-max` برای Hover شرطی، ثبات Bounds، فاصله ۴/۸ و Touch target | صفحه معامله خلوت‌تر و Hover خدمات خانه به‌صورت چهار سطح مجزا و بدون چسبیدن به قاب اصلی دیده می‌شود | CSS assertion، JavaScript Parse و `git diff --check` پاس شد؛ Touch target ۷۸px حفظ و Hover همچنان فقط برای `hover:hover/pointer:fine` فعال است |
| V-084 | 16 Aug 2026 | اقساط بین Hub قرارداد فعال و صفحه جداگانه شروع خرید جدید پخش بود و حالت واقعی کاربر در Review صریح نبود | یک Renderer و مقصد واحد برای اقساط ساخته شد؛ `WF-MM-023A` قرارداد فعال با پرداخت/تاریخچه و `WF-MM-023B` حالت بدون قرارداد با فرم شروع خرید جدید را نشان می‌دهد. دو حالت در ستون سناریو جدا و CTA شروع خرید جدید از حالت قرارداد فعال حذف شد؛ Preview/Result حفظ شدند | درخواست مستقیم مالک محصول؛ D-162؛ قواعد `ui-ux-pro-max` برای یک CTA اصلی، Empty state اقدام‌پذیر و Progressive disclosure | کاربر فقط اقدام متناسب با وضعیت خود را می‌بیند و تیم می‌تواند هر دو حالت را کنار هم Review کند، بدون صفحه Setup موازی | JavaScript Parse، دو Scenario مستقل، Renderer مشترک، حذف Route/تابع Setup و مسیر فرم→Preview بازآزمایی شد؛ Review بصری مالک محصول باز است |
| V-083 | 16 Aug 2026 | فرم برداشت هشدار بانکی جاری، موجودی کامل و راه سریع برداشت کل موجودی را نداشت؛ بازه خارج از ورودی و CTA با عنوان «بررسی برداشت» بود | Lead «برداشت از کیف» و توضیح آن حذف؛ هشدار بانک‌های ملی/تجارت/صادرات، موجودی قابل برداشت، «همه موجودی»، حداقل/حداکثر داخل کنترل مبلغ، Validation سقف موجودی و CTA «برداشت» اضافه شد | درخواست مستقیم مالک محصول | تصمیم برداشت با Context بانکی و مالی کامل‌تر و ورود کل موجودی سریع‌تر می‌شود | JavaScript Parse، وجود Handler همه موجودی، Min/Max، حذف Lead و `git diff --check` پاس شد |
| V-082 | 16 Aug 2026 | Hub خدمات تاریخچه و واریز را نداشت و ترتیب گزینه‌ها با اولویت مالک محصول هم‌راستا نبود؛ کلید آیکون واریز نیز تعریف نشده بود | تاریخچه و واریز با Routeهای موجود افزوده، ترتیب ۱۰ گزینه اصلاح و آیکون مستقل ورود وجه به کیف برای واریز تعریف شد | درخواست مستقیم مالک محصول | خدمات پرتکرار و مالی با ترتیب مشخص و نشانه بصری کامل در دسترس‌اند | JavaScript Parse، تعداد ۱۰ Tile، ترتیب DOM، وجود Routeها و آیکون واریز و `git diff --check` پاس شد |
| V-081 | 16 Aug 2026 | چهار میانبر خدمات روی خانه باید شبیه ردیف فشرده مرجع شوند، نه خود Hub خدمات | بخش خانه به یک کانتینر چهارستونه بدون Divider، با آیکون کوچک و عنوان زیر آیکون تبدیل شد؛ Variant اشتباه حذف و `WF-MM-013` بدون تغییر حفظ شد | اصلاح مستقیم مالک محصول و تصویر مرجع | میانبرهای خانه فضای کمتری می‌گیرند و Hub کامل خدمات همچنان ساختار قبلی خود را دارد | JavaScript Parse و جداسازی Home/Hub پاس شد؛ سناریو و تابع آزمایشی حذف، Strip فقط در Home و `git diff --check` بدون خطا است |
| V-080 | 16 Aug 2026 | عدد اصلی ورودی معامله نسبت به ترکیب فشرده صفحه کمی بیش از حد بزرگ بود | اندازه پایه از ۵۶ به ۴۸px و Scale طول‌های بیشتر از ۵/۷/۹ رقم به‌ترتیب از ۴۸/۴۰/۳۴ به ۴۲/۳۵/۳۰px کاهش یافت | بازخورد مستقیم مالک محصول؛ طراحی Mobile-first معامله | عدد ورودی متعادل‌تر می‌شود و اعداد طولانی بدون برهم‌زدن چیدمان باقی می‌مانند | JavaScript Parse و بررسی `git diff --check` |
| V-079 | 16 Aug 2026 | تعویض نرخ Ticker از پایین وارد می‌شد و تثبیت DOM پس از Transition می‌توانست شبیه پدیدارشدن ناگهانی دیده شود؛ موشن مرجع ورود Scrollمانند از بالا داشت | Track به دو ردیف `next/current` بازسازی شد: ابتدا روی `-38px` نرخ فعلی را نگه می‌دارد، سپس طی ۳۶۰ms با easing بدون Bounce به `0` حرکت می‌کند تا نرخ بعدی از بالا وارد و فعلی از پایین خارج شود؛ DOM پس از پایان در همان Frame نهایی تثبیت می‌شود | بازخورد مستقیم مالک محصول و ویدئوی مرجع؛ D-160؛ اصل Spatial continuity | تعویض نرخ مثل Scroll عمودی پیوسته دیده می‌شود و هیچ Fade، Flash یا پرش انتهایی ندارد | JavaScript Parse پاس شد؛ ترتیب DOM برابر `next/current`، حالت آغاز `-38px`، پایان `0` و Cleanup در ۳۸۰ms بازبینی شد؛ Guard لمس تکراری و مسیر Reduced Motion نیز حفظ شده‌اند |
| V-078 | 15 Aug 2026 | بازکردن فایل روی گوشی پنل سناریو را بالای اپ نشان می‌داد و قاب با ارتفاع ثابت ۷۸۰px می‌توانست از Viewport کوتاه‌تر/بلندتر باشد یا Body را اسکرول دهد | Mobile App-mode افزوده شد: Review panel و Statusbar ساختگی مخفی، Phone تمام عرض و `100dvh`، Body بدون Scroll، Main داخلی قابل Scroll و Header/Nav/Sheet/CTA/Toast Safe-area-aware شدند؛ Desktop Review حفظ شد | درخواست مستقیم مالک محصول؛ D-151/D-161 | هرکس فایل را روی گوشی باز کند همان تجربه اپ را می‌بیند و اختلاف ارتفاع دستگاه فقط فضای Main قابل اسکرول را تغییر می‌دهد | QA مرورگری `320×568`، `375×667`، `390×844` و `430×932` پاس شد: Phone=Viewport، Body بدون Scroll، Nav روی لبه پایین و سرریز افقی صفر. معامله در ۳۲۰×۵۶۸ با Main `436=436` و Keypad کامل بدون Scroll بود؛ Desktop ۱۲۰۰×۹۰۰ پنل، Statusbar و قاب ۳۹۰×۸۲۰ را حفظ کرد؛ Console پاک است |
| V-077 | 15 Aug 2026 | صفحه جزئیات هر فلز هم Ticker قیمت تکراری داشت، هم هویت فلز را با حرف اول نشان می‌داد و Copy «دارایی واقعی و پشتوانه‌دار» اضافه بود | Detailهای طلا/نقره/مس از لیست `LivePriceTicker` خارج شدند؛ `m.icon` جای `m.mark` را در Header Card مشترک گرفت و Copy به Grade همان فلز محدود شد | Review مستقیم مالک محصول؛ D-115/D-160 | جزئیات فلز خلوت‌تر می‌شود، هویت بصری با آیکون واقعی خوانده می‌شود و قیمت در نمودار/Context اصلی صفحه بدون تکرار باقی می‌ماند | JavaScript Parse و QA تعاملی هر سه Detail پاس شد؛ Ticker و `metal-symbol` صفر، SVG آیکون هر فلز یک و Copy ممنوع صفر بود؛ Console بدون Error/Warning است |
| V-076 | 15 Aug 2026 | `LivePriceTicker` از Header فاصله زیاد داشت و Divider زیر آن وزن بصری اضافه ایجاد می‌کرد | Padding بالای Main در صفحات دارای Ticker به ۷px کاهش یافت؛ ارتفاع Ticker به ۴۶px رسید، Divider حذف و Background خنثی `var(--strong)` با Radius ۸ و Padding افقی ۱۰px اضافه شد | Review مستقیم مالک محصول؛ D-160 | Context نرخ به Header نزدیک‌تر و به‌عنوان سطح اطلاعاتی فشرده خوانده می‌شود، بدون اینکه شبیه Card اصلی یا Section جدا باشد | JavaScript Parse و QA مرورگری پاس شد؛ فاصله Header دقیقاً ۷px، ارتفاع ۴۶px، Border صفر، Radius ۸ و Background خنثی تأیید شد. عرض‌های ۳۲۰/۳۷۵ بدون سرریز و Console بدون Error/Warning بودند |
| V-075 | 15 Aug 2026 | Context قیمت لحظه‌ای خارج از خانه/بازار/معامله سراسری نبود و کاربر در خدمات یا صفحه فلزمحور نرخ Asset جاری را نمی‌دید | `LivePriceTicker` مرکزی ۵۲px ساخته شد: صفحه عمومی طلا/نقره/مس را هر ۳٫۸ ثانیه با حرکت عمودی حلقه می‌کند، صفحه فلزمحور نرخ ثابت `assetId` را می‌گیرد، نرخ نامعتبر پیام دارد و صفحات حساس/دارای نرخ اصلی مستثنا هستند | تصمیم صریح مالک محصول و ویدئوی موشن؛ D-160؛ قواعد Motion/Accessibility `ui-ux-pro-max` | Context بازار در جریان استفاده حفظ می‌شود بدون تکرار نرخ یا پرت‌کردن تمرکز عملیات حساس | JavaScript Parse، ممیزی تعاملی ۴۱ سناریو، گردش طلا→نقره، تغییر طلا→نقره در دریافت فیزیکی، نبود Ticker در Auth و وجود نرخ داخلی معامله پاس شد؛ ۳۲۰×۷۲۰ و ۳۷۵×۸۱۲ بدون سرریز و Reduced Motion با توقف Auto پوشش داده شد؛ Review بصری مالک محصول باز است |
| V-074 | 15 Aug 2026 | متن راهنمای ثابت زیر ورودی و چهار مقدار پیشنهادی صفحه معامله شلوغی غیرضروری ایجاد می‌کردند | متن‌های حالت عادی و ردیف Preset با منطق/CSS آن‌ها حذف شدند؛ Slot زیر ورودی فقط برای Error عبور مبلغ خرید یا وزن فروش از موجودی حفظ شد و CTA در این حالت غیرفعال می‌شود | درخواست مستقیم مالک محصول؛ D-158 | صفحه خلوت‌تر می‌شود، ولی پیام خطا نزدیک همان ورودی و بدون جابه‌جایی ناگهانی Layout باقی می‌ماند | JavaScript Parse و Assertion نبود متن/Preset/Handler پاس شد؛ `role=alert`، `aria-invalid` و قفل CTA در عبور از موجودی تأیید شدند. حذف ردیف ۶۰px محتوا شرط No-scroll پاس‌شده V-073 را محافظه‌کارانه‌تر می‌کند؛ Review بصری باز است |
| V-073 | 15 Aug 2026 | Candidate معامله تأیید شد اما Prototype مرجع هنوز Tab سگمنت‌شده با آیکون، عدد راست‌چین، فاقد Preset و دارای کنترل دو‌دکمه‌ای تومان/گرم بود | بدنه Entry عین چیدمان Candidate داخل App Shell شمش بازسازی شد: Tab متنی، Toolbar نرخ و سگمنت ۱۱۸×۴۰، موجودی Contextual، عدد بزرگ مرکزچین، معادل، CTA نام فلز، چهار Preset و کیبورد همیشه‌باز؛ Header اعلان/پروفایل و Bottom Nav حفظ شدند. واحد به Label + آیکون تبدیل یک‌لمسی و بدون Listbox تبدیل شد | درخواست مستقیم مالک محصول با فایل `trade-binance-kraken-layout-candidate.html`؛ D-151/D-152/D-157؛ ممیزی `ui-ux-pro-max` | Candidate از فایل آزمایشی به تجربه مرجع منتقل می‌شود، همه کنترل‌ها در یک viewport دیده می‌شوند و تغییر واحد بدون گام/Overlay اضافه انجام می‌شود | JavaScript Parse و Playwright در ۳۲۰×۷۲۰، ۳۷۵×۸۱۲ و ۳۹۰×۸۴۴ پاس شد؛ `scrollHeight=clientHeight=622`، انتهای Keypad دقیقاً ابتدای Bottom Nav، ترتیب `1..9/decimal/0/backspace`، Preset، تبدیل مستقیم `۱٬۰۰۰٬۰۰۰ تومان → ۰٫۰۱۴ گرم` و نبود Listbox/سرریز تأیید شد؛ Review بصری مالک محصول باز است |
| V-072 | 15 Aug 2026 | سگمنت خرید/فروش Candidate هنوز بزرگ بود، Divider زیر فلزها وزن زیادی داشت، سلکت واحد در سمت راست باعث جابه‌جایی ادراکی عدد می‌شد و موجودی Contextual خرید/فروش نمایش داده نمی‌شد | سگمنت به سطح بصری ۱۱۸×۴۰ با Hit area توسعه‌یافته، Divider به خاکستری بسیار روشن، Amount control به Grid متقارن سه‌ستونه و عدد واقعاً مرکزچین تغییر کرد؛ Listbox واحد به ستون چپ منتقل شد. یک ردیف فشرده پیش از ورودی اضافه شد که در خرید موجودی کیف تومان و در فروش موجودی قابل‌فروش فلز منتخب را نشان می‌دهد؛ مبلغ/وزن بیش از موجودی CTA را می‌بندد و پیام نزدیک ورودی می‌دهد | بازخورد مستقیم مالک محصول؛ D-063/D-098/D-100/D-151/D-154؛ `Design assumption` برای جای دقیق موجودی تا Review نهایی OQ-061 | Context مالی بدون Card یا برهم‌زدن مرکز عدد قابل‌دیدن می‌شود و کاربر پیش از ادامه، منبع قابل‌خرج مرتبط با جهت معامله را می‌بیند | QA مرورگری ۳۹۰×۸۲۰، ۳۷۵×۸۱۲ و ۳۲۰×۷۲۰ پاس شد؛ مرکز عدد اختلاف صفر از محور viewport، سلکت کاملاً چپ و بدون هم‌پوشانی، تغییر موجودی خرید/فروش و عدم سرریز تأیید شد. Review جای موجودی و Recovery کمبود توسط مالک محصول باز است |
| V-071 | 15 Aug 2026 | Candidate مستقل معامله نسبت به رفرنس Binance بیش‌ازحد درشت بود؛ Asset Tab و سگمنت خرید/فروش خط و ارتفاع اضافه داشتند، شمارنده تازگی نرخ لازم نبود، کیبورد فقط پس از Focus باز می‌شد، Select واحد ظاهر پیش‌فرض HTML داشت و CTA با «بررسی» نام‌گذاری شده بود | Asset Tab به متن ۱۴px بدون Indicator و ارتفاع ۵۴px، سگمنت به ۱۳۴×۴۴، ورودی به ارتفاع ۷۶px و اندازه تطبیقی کاهش یافت؛ شمارنده نرخ حذف شد؛ CTA، پیشنهادهای واحدمحور و کیبورد داخلی از ورود اولیه ثابت‌اند؛ واحد تومان/گرم به Trigger و Listbox اختصاصی در سمت راست عدد تبدیل و CTA به «خرید/فروش + نام فلز» تغییر کرد | بازخورد تصویری مستقیم مالک محصول با رفرنس Binance و Kraken؛ D-120/D-144/D-151/D-152/D-154؛ ممیزی `ui-ux-pro-max` | نسبت‌های صفحه به رفرنس نزدیک‌تر، ورود مبلغ بی‌وقفه و نام اقدام اصلی مستقیم‌تر می‌شود؛ تصمیم جای موجودی همچنان زیر OQ-061 باز می‌ماند | QA مرورگری ۳۹۰×۸۲۰ و ۳۷۵×۸۱۲، نمایش اولیه کیبورد، ورود مبلغ پیشنهادی، Listbox تومان/گرم، تغییر فلز/خرید/فروش و نام CTA پاس شد؛ Review بصری مالک محصول باز است |
| V-070 | 15 Aug 2026 | انتخاب کارت در افزایش موجودی و صفحات روش‌ها HTML تکراری داشت و مقصد برداشت از الگوی متفاوتی استفاده می‌کرد؛ تغییر بعدی می‌توانست ظاهر و رفتار Contextها را از هم جدا کند | منبع کارت‌ها به `userBankCards` و Rendererهای مشترک `bankCardSelector`/`bankCardPicker` تبدیل شد؛ افزایش موجودی، کارت‌به‌کارت، شبا، حساب‌به‌حساب و برداشت به همان Component وصل شدند؛ حالت خالی، افزودن کارت، بازگشت به Context و Preview برداشت نیز از State مشترک تغذیه می‌شوند | درخواست مستقیم مالک محصول؛ D-156؛ ممیزی `ui-ux-pro-max` برای Consistency، Touch target و State قابل‌فهم | تغییر Component در یک محل به تمام Contextهای انتخاب کارت منتشر می‌شود و تفاوت ناخواسته ظاهر، مقصد انتخاب و رفتار افزودن حذف می‌شود | JavaScript Parse و Playwright روی پنج Context پاس شد؛ کلاس/ارتفاع ۶۴px/Radius مشترک، Picker و انتخاب مستقل واریز/برداشت، حالت خالی، افزودن و بازگشت، Preview مقصد و نبود سرریز عرض ۳۲۰/۳۹۰px بررسی شد؛ Review بصری مالک محصول باز است |
| V-069 | 15 Aug 2026 | واریز درگاهی پیش از خروج Preview داخلی نشان می‌داد؛ روش‌های دیگر بدون مبلغ قفل بودند؛ Card مقصد بانکی رنگ طلایی و ارتفاع اضافه داشت و هر سه صفحه اطلاعاتی CTA ثبت/ادامه نامتناسب داشتند | پرداخت درگاه مستقیماً به State انتقال بانکی وصل شد؛ مرور روش‌ها مستقل از مبلغ شد؛ Cardهای کارت/شبا/حساب سیاه‌وسفید، متراکم و شبیه ابزار بانکی شدند؛ CTAهای «واریز را انجام دادم» و «ادامه با بانکداری باز» حذف شدند | درخواست مستقیم مالک محصول؛ D-035/D-153/D-155؛ ممیزی `ui-ux-pro-max` برای Progressive disclosure، Touch target و تفکیک اقدام داخل/خارج اپ | مسیر اصلی یک گام کمتر دارد و صفحات روش‌های بیرونی نقش مرجع اطلاعاتی روشن‌تری پیدا می‌کنند؛ کاربر پیش از تعیین مبلغ نیز می‌تواند روش‌ها را مقایسه کند | JavaScript Parse، بازشدن هر سه روش بدون مبلغ، نبود Preview/CTA، انتقال مستقیم درگاه، خنثی‌بودن Card، Touch target حداقل ۴۴px و نبود سرریز افقی در ۳۲۰/۳۹۰px با Playwright پاس شد؛ Review بصری مالک محصول باز است |
| V-068 | 15 Aug 2026 | صفحه معامله Card و خطوط اضافه داشت، Asset Tab با Indicator خطی ناسازگار بود، نرخ از انتخاب فلز دور بود، خرید/فروش و آیکن‌ها درشت بودند، ورودی چپ‌چین و ترتیب دیداری کیبورد RTL معکوس بود و Entry در یک viewport کامل دیده نمی‌شد | فرم معامله به ساختار بدون Card تبدیل شد؛ Asset Tab به segmented فشرده بدون خط با آیکن ۲۰px تغییر کرد؛ نرخ زیر Tab، خرید/فروش و موجودی در یک Toolbar، واحد در کنترل فشرده و مقدار راست‌چین شد؛ کیبورد LTR با ترتیب استاندارد و ممیز/صفر/حذف ساخته شد؛ CTA مستقیم و بدون Container اضافه پایین صفحه قرار گرفت | درخواست مستقیم مالک محصول؛ D-151/D-152/D-154؛ ممیزی `ui-ux-pro-max` برای Touch target، فرم موبایل و جلوگیری از بریدن محتوا | کل Entry از انتخاب فلز تا CTA در Portrait استاندارد بدون اسکرول دیده می‌شود و سلسله‌مراتب نرخ/مقدار/اقدام آرام‌تر و روشن‌تر است | JavaScript Parse، ورود رقم/ممیز/حذف، ترتیب هندسی کلیدها، راست‌چین‌بودن ورودی، Touch target حداقل ۴۴px و No-scroll در ۳۲۰×۷۲۰، ۳۷۵×۸۱۲ و ۳۹۰×۸۴۴ با Playwright پاس شد؛ Review بصری مالک محصول باز است |
| V-067 | 15 Aug 2026 | سایر روش‌های واریز پس از انتخاب داخل همان صفحه مبلغ باز می‌شدند، عنوان سکشن تکراری داشتند و جزئیات بانکی هر روش/بازگشت مستقل روشن نبود؛ کارت مبدأ نیز عبارت زائد «پیش‌فرض انتخاب شده» داشت | عبارت زائد و Header سکشن روش‌ها حذف شد؛ Trigger واحد «روش‌های دیگر واریز» حفظ و سه صفحه مستقل کارت‌به‌کارت، شبا و حساب‌به‌حساب با Back ساخته شد؛ کارت/مبلغ در رفت‌وبرگشت حفظ می‌شوند؛ هشدار مالکیت، پل/پایا، شناسه و توضیحات الزامی، اطلاعات مقصد، Copy و SLA نوع‌محور اضافه شدند | درخواست مستقیم مالک محصول و سه رفرنس تصویری `Increase Wallet`؛ D-035/D-058/D-088/D-153؛ ممیزی `ui-ux-pro-max` برای Back قابل پیش‌بینی، Touch target و Progressive disclosure | فرم مبلغ سبک می‌ماند و کاربر جزئیات حساس هر روش را در Context مستقل، قابل بازگشت و نزدیک‌تر به الگوی اپ موبایل می‌بیند | JavaScript Parse، مسیرهای تعاملی انتخاب/Back/حفظ مبلغ/Preview و سرریز عرض‌های ۳۲۰/۳۷۵/۳۹۰ و Landscape با Playwright پاس شد؛ Review بصری مالک محصول باز است |
| V-066 | 15 Aug 2026 | باکس پایینی جزئیات فلز پس از حذف اقدام‌های فرعی هنوز اطلاعات کم‌کاربرد و سنگین نشان می‌داد | کل باکس «نرخ خرید از کاربر / سود تحقق‌یافته / قابلیت‌ها» از جزئیات فلز حذف شد و CTA معامله بلافاصله پس از محتوای اصلی صفحه در جریان عادی اسکرول باقی ماند | درخواست مستقیم مالک محصول در Review جزئی صفحه جزئیات فلز؛ ادامه ساده‌سازی V-065 | صفحه جزئیات کوتاه‌تر و متمرکزتر می‌شود و کاربر بدون برخورد با اطلاعات مبهم به اقدام اصلی معامله می‌رسد | JavaScript Parse و جست‌وجوی ساختاری حذف باکس انجام شد؛ Review بصری/لمسی مالک محصول باز است |
| V-065 | 15 Aug 2026 | اعلان‌ها با Card و دکمه جدا شبیه محتوای عمومی بودند؛ عنوان عملکرد کارت فلز جهت سود/زیان را صریح نمی‌کرد؛ جزئیات فلز دو اقدام فرعی اضافه و CTA ثابت داشت | اعلان‌ها به ردیف استاندارد تمام‌عرض با آیکن، عنوان، توضیح، زمان و نشان خوانده‌نشده تبدیل شدند؛ برچسب عملکرد کارت بر اساس علامت داده «سود کل/زیان کل» شد؛ «سوابق این فلز» و «دریافت فیزیکی» از جزئیات حذف و CTA معامله بعد از آخرین Card در جریان عادی اسکرول قرار گرفت | درخواست مستقیم مالک محصول؛ ادامه Review جزئی MM-W1 و قرارداد Touch-first D-151/D-152 | اسکن اعلان‌ها سریع‌تر و مقصد کل ردیف قابل لمس است؛ معنای P&L بدون ابهام خوانده می‌شود؛ صفحه جزئیات با یک اقدام اصلی روشن و بدون نوار ثابت پایان می‌یابد | JavaScript Parse، ممیزی ساختاری برچسب‌های سود/زیان، حذف CTAهای فرعی جزئیات و `git diff --check` انجام شد؛ Review بصری/لمسی مالک محصول باز است |
| V-064 | 15 Aug 2026 | صفحه واریز عنوان بدنه تکراری داشت، انتخاب روش داخل Card مبلغ بود و مبدأ پرداخت/State بدون کارت مشخص نبود | Header به «افزایش موجودی کیف پول» تغییر کرد و Lead تکراری حذف شد؛ Card مبلغ فقط ورودی/مبالغ پیشنهادی را نگه داشت؛ انتخاب کارت مبدأ با حالت بدون کارت، کارت پیش‌فرض، انتخاب کارت دیگر و افزودن کارت ساخته شد؛ «روش‌های دیگر واریز» بیرون Card و مبلغ‌محور شد؛ کارت‌به‌کارت، شبا با تب پل/پایا و حساب‌به‌حساب Open Banking پنج قابلیت D-035 را همراه درگاه حفظ می‌کنند | درخواست مستقیم مالک محصول؛ Figma `Increase Wallet` node `8814:88200` و فریم‌های `8839:95874`، `8839:96115`، `8827:94990`؛ D-035/D-058 | شروع کوتاه‌تر، کاهش خطای مبدأ، نمایش فقط روش‌های متناسب و حفظ مقایسه Scan عمودی مالی | JavaScript با `node --check` و `git diff --check` پاس شد؛ Stateهای `deposit` و `deposit-no-card`، Sheet کارت/روش، بازگشت افزودن کارت و پل/پایا ممیزی ساختاری شدند؛ Review بصری/لمسی مالک محصول باز است |
| V-063 | 15 Aug 2026 | صفحه معامله هنوز ورودی وابسته به جهت خرید/فروش و کیبورد سیستم داشت؛ موجودی کیف بیش‌ازحد برجسته بود، جزئیات محاسبه جای مستقلی نداشت و تب فلز/آیکن معامله وضوح کافی نداشتند | آیکن چشم دارایی کل سفید و آیکن ناوبری معامله به Swap افقی تبدیل شد؛ Tabهای طلا/نقره/مس با آیکن شمش، سکه و کویل و بدون نماد شیمیایی بازطراحی شدند؛ ورودی readonly دوحالته تومان/گرم با کیبورد داخلی و پشتیبانی کیبورد فیزیکی وب ساخته شد؛ موجودی به ردیف فشرده بدون عیار تبدیل و جزئیات محاسبه به Bottom Sheet کنار نرخ منتقل شد؛ CTA در حالت خالی نیز نام «خرید/فروش» را حفظ می‌کند | درخواست مستقیم مالک محصول و شاهد محلی صفحه خرید/فروش Wallgold؛ ادامه D-151/D-152 برای Touch-first و Progressive Enhancement | ورود مقدار بدون بازشدن کیبورد سیستم، تبدیل روشن تومان/گرم، اسکن سریع‌تر Context مالی و دسترسی کم‌اصطکاک به سه فلز | JavaScript با `node --check` پاس شد؛ جست‌وجوی ساختاری حذف دو متن قبلی و CTA «مبلغ را وارد کنید» از معامله را تأیید کرد؛ کنترل بصری خودکار `file://` به‌علت سیاست امنیتی مرورگر انجام نشد و Review بصری/لمسی مالک محصول باز است |
| V-062 | 15 Aug 2026 | صفحه معامله پیش از فرم Lead تکراری داشت و انتخاب فلز سه‌تایی به‌صورت Select/Sheet دیده می‌شد | تیتر «خرید/فروش فلز» و توضیح زیر آن از صفحه معامله حذف شد؛ انتخاب طلا، نقره و مس به Tablist سه‌ستونه با نشان فلز، نام، نماد لاتین و Indicator زیرین تبدیل شد؛ فرم معامله به Tabpanel متصل شد، پیمایش کیبوردی چپ/راست و Home/End اضافه شد و منطق هشدار تغییر فلز هنگام Quote حفظ شد | بازخورد مستقیم مالک محصول؛ ادامه بازبینی MM-W3 برای کاهش مقدمه و دسترسی مستقیم به سه دارایی فعال | صفحه معامله فشرده‌تر آغاز می‌شود و جابه‌جایی میان سه فلز بدون بازکردن Sheet انجام می‌شود؛ ظاهر Asset Tab با Segment خرید/فروش و تب‌های کپسولی دیگر تفاوت روشن دارد | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری حذف Lead/Selector و وجود Tablist سه‌فلزی را تأیید کرد |
| V-061 | 15 Aug 2026 | با وجود افزایش اندازه Strong، عدد کنار «گرم» در خروجی هنوز بسیار کوچک‌تر دیده می‌شد | انتخابگرهای عمومی `.portfolio-card-balance span` و `.wallet-balance-copy span` به فرزند مستقیم (`>span`) محدود شدند تا فقط Label کوچک شود و `balance-number` اندازه ۲۲px کارت فلز و ۲۰px کیف را از Strong به ارث ببرد؛ «گرم» همچنان با ضریب `.72em` کوچک می‌ماند | اسکرین‌شات مستقیم مالک محصول نشان داد قانون Label، عدد تو‌در‌تو را ناخواسته به ۱۰px و ۱۱px کاهش می‌دهد | عدد موجودی فلز واقعاً درشت‌تر از «گرم» نمایش داده می‌شود و عدد کیف نیز اندازه تعریف‌شده را حفظ می‌کند | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری انتخابگرهای فرزند مستقیم را تأیید کرد |
| V-060 | 15 Aug 2026 | عدد اصلی موجودی گرم فلزات و موجودی تومان کیف پول هنوز وزن بصری کافی نداشت | اندازه عدد اصلی کارت‌های فلزات از ۲۰ به ۲۲px و عدد موجودی کیف پول از ۱۸ به ۲۰px افزایش یافت؛ اندازه واحد «گرم» و متن‌های فرعی بدون تغییر باقی ماند | بازخورد مستقیم مالک محصول؛ ادامه D-150/V-059 برای تقویت سلسله‌مراتب اطلاعات مالی | موجودی اصلی روی هر دو نوع کارت سریع‌تر و واضح‌تر خوانده می‌شود، بدون تغییر در ترازبندی واحد یا کنترل چشم | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری اندازه‌های ۲۲px و ۲۰px را تأیید کرد |
| V-059 | 15 Aug 2026 | عدد وزن فلز و موجودی تومان روی کارت کیف کمی کوچک‌تر از وزن بصری مطلوب بود و نشان ورود کیف/بنر باید chevron-left بدون قاب باشد | عدد اصلی موجودی فلز از ۱۸ به ۲۰px و موجودی کیف از ۱۶ به ۱۸px افزایش یافت؛ آیکون مشترک `chevronLeft` اضافه شد و روی کارت کیف و بنر کمپین جایگزین نشان قبلی شد؛ Border و Background نشان بنر حذف شد | بازخورد مستقیم مالک محصول؛ ادامه D-150/V-058 برای خوانایی عدد اصلی و affordance سبک | عددهای اصلی سریع‌تر خوانده می‌شوند و نشان ورود کیف/بنر با الگوی سبک و بی‌کادر هماهنگ می‌شود | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری اندازه‌های جدید، `chevronLeft` و `campaign-action` بدون border/background را تأیید کرد |
| V-058 | 15 Aug 2026 | چشم کارت باید دقیقاً هم‌راستا و چسبیده به واحد «گرم» باشد، Hover اضافه نداشته باشد و کوچک‌سازی واحد نباید عدد وزن یا معادل تومانی را ضعیف کند | فقط واحد `گرم` با کلاس `balance-unit gram` کوچک‌تر شد؛ واحدهای دیگر اندازه و رنگ عدد را حفظ می‌کنند؛ معادل تومانی کارت‌ها از خاکستری/ریز به عدد خواناتر برگشت؛ Hover چشم حذف شد؛ جای چشم با `width/top/height` ردیف مقدار پس از Render هم‌راستا می‌شود | بازخورد مستقیم مالک محصول؛ ادامه D-150/V-057 برای Proximity دقیق و خوانایی اعداد مالی | چشم کنار همان عبارت «گرم» دیده می‌شود، بدون رفتار hover اضافه؛ عدد وزن و معادل ریالی دوباره وزن بصری کافی دارند | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری حذف `balance-toggle:hover` و وجود `balance-unit.gram` و `balance-top` را تأیید کرد |
| V-057 | 15 Aug 2026 | فاصله چشم از واحد مقدار هنوز باید دقیق‌تر می‌شد، واحد «گرم» در کارت‌ها کمی درشت بود، اسکرول‌بار عمودی خانه لازم نبود و کارت کیف نشانه ورود نداشت | مقدار و واحد به spanهای جدا تبدیل شدند؛ واحد مقدار با کلاس `balance-unit` کوچک‌تر شد؛ جای چشم بعد از هر Render با اندازه متن مقدار محاسبه می‌شود تا ۴px کنار واحد بایستد؛ اسکرول‌بار فقط برای صفحه‌های خانه مخفی شد؛ کارت کیف پول ستون سوم و نشان `>` سمت چپ گرفت | بازخورد مستقیم مالک محصول؛ ادامه D-150/V-056 برای Proximity، کاهش شلوغی و افزایش affordance کارت کیف | کنترل چشم دقیقاً به مقدار حساس وابسته دیده می‌شود، خانه تمیزتر اسکرول می‌شود و قابل‌لمس‌بودن کیف پول روشن‌تر است | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` برای فایل وایرفریم بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری `balance-unit`، `syncBalanceTogglePositions`، `home-scrollless` و `wallet-chevron` را تأیید کرد |
| V-056 | 15 Aug 2026 | پس از کلیک روی آیکن چشم، Rail کارت‌های دارایی به ابتدای اسکرول برمی‌گشت و جای چشم نیازمند تنظیم بصری نهایی بود | چشم کارت‌های دارایی به سمت چپ محدوده عدد موجودی/ارزش منتقل شد، Border و پس‌زمینه دور آیکن حذف شد و Padding چپ برای جلوگیری از برخورد عدد با آیکن رزرو شد؛ Handler نمایش/مخفی‌سازی قبل از `render()` مقدار `scrollTop` صفحه و `scrollLeft` همان Rail را ذخیره و بعد از Render بازیابی می‌کند | بازخورد مستقیم مالک محصول؛ ادامه D-150/V-055 برای Proximity کنترل، کاهش کادر اضافه و حفظ موقعیت پیمایش افقی | کاربر هنگام آشکارسازی موجودی همان کارت، جای خود را در Rail از دست نمی‌دهد و کنترل چشم سبک‌تر و نزدیک محدوده مقدار حساس دیده می‌شود | JavaScript داخل HTML با `new Function` parse شد؛ `git diff --check` برای فایل وایرفریم بدون خطای whitespace پاس شد؛ جست‌وجوی ساختاری وجود `railScrollLeft`، `border:0` و موقعیت چپ چشم را تأیید کرد |
| V-055 | 15 Aug 2026 | آیکن چشم در بالای کارت‌ها حس کنترل عمومی کارت می‌داد و از خود مقدار موجودی فاصله داشت | جای چشم کارت‌های دارایی از Header به پایین کارت و کنار محدوده موجودی/ارزش منتقل شد؛ چشم کیف تومان در میانه ارتفاع کارت و نزدیک عدد موجودی قرار گرفت؛ فاصله رزرو‌شده کنار سود/زیان حذف شد | بازخورد مستقیم مالک محصول؛ ادامه D-150 و بازبینی `ui-ux-pro-max` برای Proximity و ارتباط کنترل با داده حساس | کاربر سریع‌تر می‌فهمد چشم فقط همان موجودی را نمایش/مخفی می‌کند و Header کارت خلوت‌تر می‌شود | JavaScript Parse پاس شد؛ جست‌وجوی ساختاری موقعیت پایین چشم، موقعیت میانی چشم کیف و حذف فاصله اضافی P&L را تأیید کرد؛ `git diff --check` برای فایل وایرفریم بدون خطای whitespace پاس شد |
| V-054 | 11 Aug 2026 | صفحه خانه در ورود اولیه عددهای حساس کاربر را فوراً نمایش می‌داد و زیر گرید چهار Shortcut جای مشخصی برای پیام‌های متغیر کمپین وجود نداشت | مقدارهای دارایی کل، طلا، نقره، مس و کیف تومان به‌صورت پیش‌فرض ماسک شدند؛ هر کارت کنترل مستقل چشم گرفت؛ ساختار کارت‌ها برای جلوگیری از Button تو در تو به `article + button اصلی + button چشم` تغییر کرد؛ زیر چهار Shortcut یک Slider بنر کمپین سه‌اسلایدی اضافه شد | درخواست مستقیم مالک محصول — D-150؛ بازبینی `ui-ux-pro-max` برای حریم خصوصی، Icon-only label، Touch target و Gesture conflict | صفحه خانه برای نمایش در فضای عمومی امن‌تر است، کاربر کنترل محلی روی افشای هر موجودی دارد و مارکتینگ جایگاه قابل تعویض بدون رقابت با CTA خرید/فروش می‌گیرد | JavaScript Parse پاس شد؛ جست‌وجوی ساختاری وجود state مخفی پیش‌فرض، کنترل‌های چشم، ساختار مستقل کارت/کیف، Slider کمپین و Handlerهای Toggle/Campaign را تأیید کرد؛ `git diff --check` بدون خطای whitespace پاس شد |
| V-053 | 11 Aug 2026 | CTA «خرید فلز» دامنه فروش را منعکس نمی‌کرد، از Rail فاصله و ارتفاع بیشتری داشت و عنوان/آیکن جداگانه دارایی ورودی تکراری می‌ساخت | CTA به «خرید و فروش» با ارتفاع ۴۴px و فاصله ۴px تبدیل شد؛ Padding پایین Rail حذف شد؛ عنوان و آیکن سکشن دارایی حذف شدند؛ کارت دارایی کل ورودی `portfolio-mix` شد | درخواست مستقیم مالک محصول — D-149؛ بازبینی `ui-ux-pro-max` برای Touch target و کاهش تکرار | خانه فشرده‌تر، مسیر معامله دقیق‌تر و ورود صفحه دارایی‌ها با خود Summary قابل‌کشف می‌شود | JavaScript Parse و ۱۲ Assertion CTA جدید/حذف قدیمی، فاصله، Touch target، حذف Header، دسترس‌پذیری سکشن، Button کارت کل، مقصد و ورود پیش‌فرض معامله پاس شدند |
| V-052 | 11 Aug 2026 | سه Variant V-051 برای انتخاب ساخته شده بودند و نگه‌داشتن مدل‌های ردشده فایل ارائه را شلوغ می‌کرد | Variant A «سود/زیان روبه‌روی عنوان» به خانه اصلی Promote شد؛ Variantهای میانه و پایین، سناریوها و شاخه‌های شرطی آن‌ها حذف شدند؛ Label، Trend Arrow، حذف sign و State استثنایی حفظ شد | تأیید صریح مالک محصول پس از مقایسه — D-148 | Prototype دوباره یک خانه مرجع و ۳۷ سناریوی محصولی دارد و تصمیم نهایی کارت بدون کد آزمایشی اضافه ارائه می‌شود | JavaScript Parse و ۱۲ Assertion شمارش ۳۷ سناریو، حذف کامل B/C، جای P&L، Label، دو Arrow، حذف sign، وزن/ارزش و State استثنایی پاس شدند |
| V-051 | 11 Aug 2026 | جای سود/زیان و نمایش آن با علامت مثبت/منفی رضایت‌بخش نبود و مقایسه جایگاه‌ها در یک State ممکن نبود | سه سناریوی A/B/C برای جایگاه سود/زیان ساخته شد: روبه‌روی عنوان، میانه کارت و پایین کارت. هر سه Label «سود/زیان» دارند و مقدار با آیکن SVG Trend شکسته بالا/پایین نمایش داده می‌شود؛ علامت مثبت/منفی از مقدار و درصد حذف شد؛ نرخ ناقص/توقف بدون آیکن در همان Slot می‌آید | درخواست مستقیم مالک محصول برای آزمودن چند جایگاه؛ بازبینی `ui-ux-pro-max` برای مقایسه سلسله‌مراتب | تصمیم نهایی جایگاه با مقایسه زنده سه Variant گرفته می‌شود، بدون تغییر اطلاعات یا مسیر خرید | JavaScript Parse و ۱۲ Assertion تعداد ۴۰ سناریو، سه Variant، جهت دو Arrow، حذف sign، Label، جایگاه‌ها و State استثنایی پاس شدند؛ انتخاب نهایی مالک محصول باز است |
| V-050 | 11 Aug 2026 | دو ردیف مستقل «ارزش فعلی / سود‌وزیان» هنوز پایین کارت را شبیه جدول و شلوغ نشان می‌دادند | عنوان «ارزش فعلی» و Footer جدولی حذف شد؛ مبلغ تومانی بی‌واسطه زیر وزن قرار گرفت و مقدار سود/زیان به Header روبه‌روی نام/عیار منتقل شد؛ در نرخ ناقص/توقف همان جایگاه پیام وضعیت را می‌گیرد | ایده و تأیید جهت از مالک محصول؛ بازبینی `ui-ux-pro-max` برای Proximity و حذف Label قابل‌استنباط | کارت دو ناحیه روشن دارد: Header هویت+عملکرد و Body موجودی+معادل تومان؛ اطلاعات مالی حفظ و اسکن سریع‌تر می‌شود | JavaScript Parse و ۱۲ Assertion ساختار دوناحیه‌ای، جای P&L، مبلغ زیر وزن، حذف Footer، حفظ اعداد، Alignment و State استثنایی پاس شدند؛ Review بصری مالک محصول باز است |
| V-049 | 11 Aug 2026 | با وجود حذف آیکن و Divider، هویت و دو ستون اطلاعات مالی هنوز چند نقطه توجه هم‌زمان می‌ساختند و کارت ساختار خواندن واضحی نداشت | کارت به مسیر عمودی ثابت بازطراحی شد: نام+عیار، موجودی به‌عنوان عدد اصلی، سپس دو ردیف Label/Value هم‌تراز برای ارزش فعلی و سود/زیان؛ Symbol لاتین از نمای فشرده حذف و وضعیت استثنایی به متن کوتاه کنار عیار تبدیل شد | بازخورد مستقیم مالک محصول؛ بازبینی `ui-ux-pro-max` برای Single focal point و Alignment | اسکن کارت از بالا به پایین قابل‌پیش‌بینی می‌شود و اطلاعات مالی بدون رقابت ستونی یا تزئین اضافه باقی می‌مانند | JavaScript Parse و ۱۳ Assertion ترتیب عمودی، Alignment، عدد اصلی، حفظ وزن/ارزش/P&L، حذف Icon/Symbol/Divider و Stateها پاس شدند؛ Review بصری مالک محصول باز است |
| V-048 | 11 Aug 2026 | کوچک‌شدن قاب کارت‌ها کافی نبود؛ ترکیب آیکن فلز، عنوان، نماد، عیار و خط جداکننده هنوز محتوای داخل را شلوغ و لایه‌لایه نشان می‌داد | بدون حذف داده، آیکن و Accent بالای کارت حذف شدند؛ نام/نماد/عیار در یک خط ساده ادغام، موجودی در یک خط فشرده و خط جداکننده Footer حذف شد؛ ارزش فعلی، سود/زیان و Stateهای استثنایی حفظ شدند | بازخورد مستقیم مالک محصول؛ بازبینی `ui-ux-pro-max` برای Minimal hierarchy و حذف تزئینات بدون کاهش اطلاعات | کارت‌ها در نگاه اول سبک‌تر و منسجم‌تر می‌شوند، اما تمام اطلاعات تصمیم‌ساز قبلی را نگه می‌دارند | JavaScript Parse و ۱۴ Assertion حفظ نام/نماد/عیار/وزن/ارزش/P&L، حذف آیکن/Divider/Accent و Stateهای عادی/استثنایی پاس شدند؛ Review بصری مالک محصول باز است |
| V-047 | 11 Aug 2026 | خانه CTA مستقیم خرید نداشت و ورود معامله فقط از Bottom Navigation یا مسیرهای Contextual قابل کشف بود؛ کارت‌های فلز نیز نسبت به محتوایشان فضای بصری و Status عادی تکراری داشتند | CTA تمام‌عرض «خرید فلز» بلافاصله زیر Rail و پیش از کیف تومان اضافه و به حالت خرید با طلای پیش‌فرض متصل شد؛ عرض/ارتفاع/Padding/تایپوگرافی/سایه کارت‌ها کاهش یافت و «فعال» فقط از حالت عادی حذف شد؛ نرخ ناقص و توقف باقی ماندند | تأیید مستقیم مالک محصول؛ D-147؛ بنچمارک قبلی خانه رقبا؛ `ui-ux-pro-max` برای سلسله‌مراتب CTA و Touch target | خرید در اولین محدوده خانه دیده می‌شود، بدون تکرار فروش یا افزودن CTA به هر کارت؛ Rail فشرده‌تر و قابل‌اسکن‌تر است | JavaScript Parse و ۱۱ Assertion جای CTA، حالت خرید، طلای پیش‌فرض، پاک‌سازی Draft، مقصد، ابعاد و Stateهای استثنایی پاس شدند؛ Review بصری مالک محصول باز است |
| V-046 | 11 Aug 2026 | نوار اسکرول مرورگر زیر کارت‌های افقی دارایی دیده می‌شد و ظاهر مینیمال خانه را شلوغ می‌کرد | Scrollbar فقط برای Rail کارت‌های دارایی در Firefox، Chromium/WebKit و مرورگرهای قدیمی Microsoft مخفی شد؛ خود پیمایش افقی، Scroll Snap و حفظ موقعیت بدون تغییر باقی ماند | بازخورد مستقیم مالک محصول؛ قاعده `ui-ux-pro-max` برای حذف Chrome بصری غیرضروری بدون حذف تعامل | کارت‌ها تمیزتر دیده می‌شوند و کاربر همچنان با لمس، Trackpad یا Wheel افقی میان آن‌ها حرکت می‌کند | CSS و قرارداد `overflow-x:auto`، `scroll-snap-type` و مخفی‌سازی سه‌موتوره بازآزمایی شد؛ Review بصری مالک محصول باز است |
| V-045 | 11 Aug 2026 | جای فیزیکی کنترل‌های Header به جهت ارث‌رسیده RTL در Grid وابسته بود و می‌توانست دکمه بازگشت/پروفایل را برخلاف قرارداد جابه‌جا کند؛ نام شناسه‌های قبلی نیز نقش و سمت را باهم مخلوط می‌کرد | Grid هدر به سه Area صریح `left/title/right` با جهت فیزیکی LTR تبدیل شد و عنوان RTL ماند؛ Root پروفایل چپ و اعلان راست دارد؛ Subpage فقط بازگشت راست با Chevron رو به راست دارد؛ شناسه‌ها و Handlerها به `header-left/header-right` تغییر کردند | تأیید صریح مالک محصول؛ D-140/D-146؛ بازبینی `ui-ux-pro-max` برای ثبات مکانی، Touch target و Label دسترس‌پذیر | حافظه مکانی Header در همه صفحات ثابت می‌ماند و RTL دیگر سمت کنترل‌ها را ناخواسته معکوس نمی‌کند | JavaScript Parse و ۱۰ Assertion مربوط به DOM، CSS، Slotها، Role/Handler، Label دسترس‌پذیر و جهت Chevron پاس شدند؛ Review بصری مالک محصول باز است |
| V-044 | 11 Aug 2026 | کارت‌های خدمات توضیح و ارتفاع اضافه داشتند؛ کیف خانه Trigger کوچکی روی Chevron داشت و Sheet موجودی را تکرار می‌کرد؛ Shortcutهای خانه در الگوی List و Grid جدا بودند | توضیح زیر عنوان Service tileها حذف و ارتفاع از ۱۳۲ به ۱۱۲ کاهش یافت؛ کل کارت کیف بدون Chevron/Label اضافه Trigger شد و Sheet فقط واریز/برداشت را نگه داشت؛ تاریخچه، خرید قسطی، دریافت فیزیکی و سفارش قیمت در گرید دو‌ستونه هم‌شکل خدمات قرار گرفتند | درخواست صریح مالک محصول؛ D-145؛ قواعد `ui-ux-pro-max` برای Touch target، تراکم و ثبات الگو | خانه ساده‌تر، سطح لمس بزرگ‌تر و Shortcutهای هم‌اهمیت قابل‌اسکن‌تر می‌شوند | JavaScript Parse و ۱۰ Assertion Trigger کامل کارت، Sheet مینیمال، حذف متن‌ها، چهار Shortcut، ارتفاع و Touch target پاس شدند؛ Review بصری مالک محصول باز است |
| V-043 | 11 Aug 2026 | CTA اصلی بعضی فلوها مانند واریز در ادامه محتوا قرار داشت و جای آن بین صفحه‌های کوتاه/بلند یکسان نبود | قرارداد D-144 اضافه شد: Action group مستقیم همه صفحه‌ها خودکار به نوار پایین Safe-area-aware تبدیل می‌شود؛ CTAهای نتیجه دریافت فیزیکی و نتیجه عمومی از داخل Card خارج شدند؛ Footer شیت‌ها و اقدام‌های Contextual بدون تغییر ماندند | درخواست صریح مالک محصول؛ D-101/D-144؛ قواعد `ui-ux-pro-max` برای Safe Area، Touch target و Fixed-element offset | پیش‌بینی‌پذیری بیشتر محل اقدام نهایی، دسترسی سریع در صفحه‌های کوتاه/بلند و جلوگیری از پنهان‌شدن CTA یا محتوا | JavaScript Parse و ۹ Assertion قرارداد CSS/DOM، Safe Area، Sheet Footer، نتیجه‌ها و پوشش ۲۷ خانواده فلو پاس شدند؛ Review بصری مالک محصول باز است |
| V-042 | 11 Aug 2026 | کارت کیف خانه به صفحه مستقل مدیریت کیف می‌رفت و واریز کاربر را پیش از فرم درگیر انتخاب روش می‌کرد | CTA متنی با فلش ساده جایگزین شد؛ Bottom Sheet موجودی + واریز/برداشت ساخته شد؛ مقصد مستقل و سناریوی `WF-MM-R03` حذف شد؛ واریز مستقیم با درگاه پیش‌فرض به فرم موجودی/مبلغ/چهار Preset/هشدار مالکیت کارت می‌رود و پنج روش قبلی پشت «تغییر روش» حفظ شدند | D-142؛ نودهای Figma `8839:95874` و `8854:98119` از سکشن `Increase Wallet` | کاهش یک سطح ناوبری و سریع‌شدن مسیر اصلی واریز، بدون حذف مسیرهای بانکی قبلی | JavaScript Parse و ۸ Assertion حذف صفحه/Sheet/Default/Presets/Warning/Methods/Responsive پاس شدند؛ Review تعاملی مالک محصول باز است |
| V-041 | 11 Aug 2026 | Accentهای طلایی/نقره‌ای/مسی، آبی کیف و سبز/قرمز مالی با جهت بصری مطلوب خانه و جزئیات دارایی هم‌راستا نبودند | فقط خانه و `WF-MM-002A` در Scopeهای مستقل Monochrome شدند؛ Accent کارت‌ها، کیف، status، سود/زیان و Alert خنثی و Donut/نشانه‌ها با چهار سطح خاکستری بازسازی شدند؛ متن، علامت و درصد برای فهم بدون رنگ حفظ شد | تصمیم D-141؛ بازبینی `ui-ux-pro-max` | تمرکز بیشتر روی عدد و ساختار، با حفظ تشخیص اطلاعات بدون اتکا به Hue | JavaScript Parse و ۷ Assertion Scope/کیف/status/Donut/Hover پاس شدند؛ Review کنتراست بصری مالک محصول باز است |
| V-040 | 11 Aug 2026 | در صفحات اصلی پروفایل چپ و اعلان راست بود، اما ورود به زیرصفحه پروفایل را به راست منتقل می‌کرد و حافظه مکانی Header را می‌شکست | Header دوحالت شد: Root = پروفایل چپ + اعلان راست؛ Subpage = چپ خالی + بازگشت راست. نقش و Handler هر Slot نیز مستقل و صریح شد و مخفی‌سازی‌های موردی زیرصفحه‌های پروفایل حذف شدند | تصمیم D-140؛ بازخورد مستقیم مالک محصول | ناوبری قابل‌پیش‌بینی، حذف پرش پروفایل و کاهش لمس اشتباه | JavaScript Parse و Assertionهای Role/Visibility/Handler و حذف Overrideهای قدیمی پاس شدند؛ Review تعاملی Back و اعلان باز است |
| V-039 | 11 Aug 2026 | کارت‌های خانه با وجود تراکم مناسب، حس Card مستقل کمی داشتند و کیف تومان از نظر بصری به‌وضوح از دارایی فلزی تفکیک نمی‌شد | یک Variant برگشت‌پذیر روی استایل پایه ساخته شد: کارت فلزها Accent باریک، سطح، کادر و سایه محدود گرفتند؛ کیف تومان به کارت مستقل دارای آیکن کیف، عنوان، موجودی قابل استفاده و CTA «مدیریت کیف پول» تبدیل شد؛ نسخه پایه با حذف Modifier قابل بازیابی است | درخواست آزمایشی مالک محصول؛ بازبینی `ui-ux-pro-max` | تشخیص سریع‌تر Cardها و تفاوت معنایی فلز با کیف، بدون افزایش تزئین یا ازبین‌بردن نسخه قبلی | JavaScript Parse، ساختار SVG، CTA حداقل ۴۴px، Modifier برگشت‌پذیر و چیدمان ۳۲۰px بررسی شدند؛ این Variant هنوز نیازمند تأیید بصری مالک محصول است |
| V-038 | 11 Aug 2026 | عدد جمع دارایی و سطح پس‌زمینه تیره نمودار نسبت به محتوای صفحه بزرگ بود | اندازه مبلغ از ۲۴ به ۲۰، Donut از ۱۴۲ به ۱۲۲ پیکسل و Padding/Gap کارت نمودار کاهش یافت؛ محدوده تشخیص Hover با حلقه جدید همگام شد | بازخورد مستقیم مالک محصول روی `WF-MM-002A` | تراکم متعادل‌تر صفحه ترکیب بدون از دست‌رفتن تعامل نمودار | JavaScript Parse و Assertion اندازه‌ها و محدوده Hover پاس شدند؛ Review بصری مالک محصول باز است |
| V-037 | 11 Aug 2026 | ارتباط هر قطعه Donut Chart با کارت جزئیات پایین فقط از طریق رنگ قابل حدس بود | موقعیت Pointer روی حلقه به سهم طلا/نقره/مس/کیف تومان نگاشت شد و کارت متناظر با تغییر پس‌زمینه، کادر، سایه و جابه‌جایی محدود برجسته می‌شود؛ خروج از حلقه حالت را پاک می‌کند | درخواست مستقیم مالک محصول روی `WF-MM-002A` | ارتباط نمودار و جزئیات بدون افزودن Legend تکراری روشن می‌شود | JavaScript Parse و Assertion نگاشت چهار بازه، شناسه کارت و پاک‌سازی Hover پاس شدند؛ Review تعاملی مالک محصول باز است |
| V-036 | 11 Aug 2026 | Legend کنار Donut Chart اطلاعات سهمی را تکرار می‌کرد که در کارت‌های زیر نمودار وجود دارد | Legend کنار نمودار حذف و Donut در مرکز کارت قرار گرفت؛ نام، ارزش و درصد سهم فقط در کارت‌های پایین باقی ماند | بازخورد مستقیم مالک محصول روی `WF-MM-002A` | کاهش تکرار و تمرکز بیشتر روی نمودار و فهرست جزئیات | JavaScript Parse و Assertion حذف Legend/حفظ کارت‌ها پاس شدند؛ Review بصری مالک محصول باز است |
| V-035 | 11 Aug 2026 | Pie Chart توپر و راهنمای جدا از آن، ارتباط بصری و نظم کافی نداشت | نمودار به Donut Chart با مرکز خالی، خلاصه ۱۰۰٪ در مرکز و Legend ستونی متصل شامل رنگ، نام و درصد هر دارایی تبدیل شد | بازخورد مستقیم مالک محصول روی `WF-MM-002A` | مقایسه سهم‌ها و ارتباط رنگ نمودار با نام دارایی سریع‌تر و منظم‌تر می‌شود | JavaScript Parse و Assertionهای حلقه، مرکز و Legend پاس شدند؛ سپس Legend تکراری در V-036 حذف شد |
| V-034 | 11 Aug 2026 | کارت‌های دارایی پس از کاهش اول هنوز بزرگ حس می‌شدند و نمایش سهم دارایی‌ها در مقصد فلش به‌صورت نوار خطی بود | ارتفاع کارت‌ها از ۱۳۲ به ۱۱۶ پیکسل، عرض، Padding، نماد، عدد موجودی و Badge کوچک‌تر شد؛ نمودار نواری ترکیب دارایی با Pie Chart و راهنمای رنگ طلا/نقره/مس/کیف تومان جایگزین شد | بازخورد مستقیم مالک محصول؛ ادامه ممیزی `ui-ux-pro-max` | نمایش بخش بیشتری از خانه و فهم سریع‌تر نسبت دارایی‌ها در صفحه ترکیب | JavaScript Parse و Assertionهای اندازه/Pie/حذف نوار پاس شدند؛ Review بصری مالک محصول باز است |
| V-033 | 11 Aug 2026 | کارت کل خانه سود/زیان تحقق‌نیافته کل و ارزش تقریبی فروش را هم‌زمان نمایش می‌داد و کارت‌های افقی فضای عمودی زیادی می‌گرفتند | کارت کل به ارزش کل دارایی محدود شد؛ کارت هر فلز وزن، ارزش فعلی و سود/زیان همان فلز را نگه داشت؛ ارتفاع، عرض داخلی، فاصله‌ها و نماد کارت‌ها کاهش یافت؛ ارزش تقریبی فروش از خانه/جزئیات عمومی حذف و دریافتی تقریبی فروش در فلو فروش روشن شد | تصمیم D-139؛ Review مستقیم مالک محصول | کاهش بار شناختی و نمایش محتوای بیشتری از خانه بدون حذف اسکرول افقی | JavaScript Parse و Assertionهای محتوا/ابعاد پاس شدند؛ Review بصری ارتفاع و خوانایی کارت‌ها باز است |
| V-032 | 11 Aug 2026 | صفحه معرفی دوستان وضعیت‌های ناموفق/در انتظار را نشان می‌داد و فهرست افراد پشت CTA و صفحه پیگیری جدا پنهان بود | خلاصه به «۳ دعوت موفق» و «مجموع جایزه دریافتی» محدود شد؛ سه دوست موفق همراه جایزه هر نفر مستقیماً زیر اشتراک لینک آمدند؛ `WF-MM-028` و مسیر پیگیری مستقل حذف شدند | تصمیم D-138 و بازخورد مستقیم مالک محصول | حذف وضعیت‌های غیرقابل‌استفاده و مشاهده نتیجه دعوت بدون ناوبری اضافه | JavaScript Parse و Assertionهای نبود مسیر مستقل/وضعیت اضافی و حضور فهرست موفق‌ها پاس شدند؛ Review بصری مالک محصول باز است |
| V-031 | 11 Aug 2026 | زیرصفحه‌های پروفایل برای ارائه تیمی کامل نبودند؛ کارت‌های بانکی نمایش ساده داشتند، افزودن کارت و تغییر رمز مسیر اجرایی نداشتند، مجوزها/قوانین مقصد مستقل نداشتند و کنتراست کد دعوت ضعیف بود | «پرونده‌های من» حذف شد؛ حساب‌های بانکی به کارت‌های بصری دارای محل لوگو تبدیل و فلو افزودن/بازبینی/ثبت کارت اضافه شد؛ تغییر رمز با اعتبارسنجی طراحی شد؛ مجوزها به گرید نام و تصویر و قوانین به صفحه متن قوانین شمش متصل شدند؛ کنتراست صفحه دعوت اصلاح شد | بازخورد مستقیم مالک محصول روی `WF-MM-029`؛ ممیزی `ui-ux-pro-max` | ارائه پیوسته‌تر پروفایل، درک سریع‌تر کارت‌ها، اقدام قابل‌آزمایش برای افزودن کارت و تغییر رمز و دسترسی روشن به اسناد اعتماد | JavaScript Parse و ۹ Assertion مسیر/محتوا پاس شدند؛ Review بصری مالک محصول روی کارت بانکی، مجوزها، متن قوانین و کنتراست دعوت باز است |
| V-030 | 11 Aug 2026 | Header و فهرست پروفایل اطلاعات نمونه ماسک‌شده، آواتار حرفی، عنوان و توضیح‌های زیاد و فلش‌های تکراری داشتند؛ پشتیبانی/پرونده‌ها ادغام و نام مجوزها طولانی بود؛ وضعیت سرویس نیز برای این مقصد لازم نبود | نام نمونه به «نیلوفر احمدی» تغییر کرد؛ موبایل و کد ملی کامل نمایش داده شدند؛ آواتار حذف و شماره/Tag احراز در یک سطر قرار گرفت؛ عنوان گروه‌ها، توضیح زیر همه آیتم‌های اصلی و فلش انتهایی حذف شدند؛ پشتیبانی و پرونده‌ها به دو ردیف مستقل تبدیل، عنوان اعتماد به «مجوزها» محدود و وضعیت سرویس از پروفایل حذف شد | بازخورد مستقیم مالک محصول روی `WF-MM-029` | اسکن سریع‌تر صفحه، تراکم کمتر و تفکیک روشن‌تر Jobهای پشتیبانی و پیگیری | JavaScript Parse و Assertionهای نام/شماره/کد ملی، حذف Avatar/Heading/فلش/وضعیت سرویس و تفکیک دو ردیف پاس شدند؛ Review بصری مالک محصول باز است |
| V-029 | 11 Aug 2026 | حساب کاربری فقط یک Bottom Sheet کوچک با نام، وضعیت احراز، تاریخچه تکراری و تنظیمات Placeholder بود؛ مسیرهای بانکی، امنیت، اعلان، پشتیبانی، اعتماد، PWA و خروج برای ارائه قابل بررسی نبودند | با D-137 حساب به سناریوی کامل `WF-MM-029` تبدیل شد؛ Avatar صفحه کامل را باز می‌کند، تاریخچه حذف و معرفی دوستان حفظ شد. محتوا در چهار گروه حساب/بانک، امنیت/اعلان، پشتیبانی/اعتماد و اپ سازمان یافت و Sheetهای تعاملی مشخصات، بانک، امنیت/2FA Candidate، اعلان‌ها، پشتیبانی، پشتوانه چندفلزی، قوانین، وضعیت سرویس، نصب و نسخه اضافه شدند؛ خروج نیز به Auth برمی‌گردد | تأیید صریح چهار تصمیم مالک محصول؛ بنچمارک ملی‌گلد/میلی/طلاسی/وال‌گلد؛ CAP-ACC-01..05، CAP-APP-01..02 و ممیزی `ui-ux-pro-max` | پروفایل از هاب خدمات جدا می‌ماند، کاربر وضعیت حساب و مسیرهای حساس را سریع‌تر پیدا می‌کند و تجربه چندفلزی بدون Selector غیرضروری حفظ می‌شود | JavaScript Parse، شمار ۳۹ سناریو، Markerهای پروفایل، Back، Toggle و خروج بازآزمایی شدند؛ Review بصری/محتوایی مالک محصول باز است |
| V-028 | 10 Aug 2026 | در خانه، لینک متنی «همه فلزها» کنار تیتر دارایی‌ها کاربر را به Market می‌برد، در حالی که نیاز Review مشاهده ترکیب دارایی بود؛ متن راهنمای «برای دیدن دارایی‌های دیگر ورق بزنید» نیز برای ارائه شلوغی اضافه ایجاد می‌کرد | لینک متنی با Icon Button بی‌پس‌زمینه و چپ‌رو جایگزین شد و به سناریوی تازه `WF-MM-002A / portfolio-mix` وصل شد؛ صفحه ترکیب دارایی پس از Review مجدد `ui-ux-pro-max` از Pie بزرگ به کارت خلاصه تیره، نوار سهم‌بندی افقی و کارت‌های سهم طلا/نقره/مس/کیف تومان تبدیل شد؛ متن راهنمای ورق‌زدن از خانه حذف شد؛ شمار سناریوها به ۳۸ به‌روز شد | بازخورد مستقیم مالک محصول روی Home و صفحه ترکیب دارایی؛ D-120/D-131؛ ممیزی `ui-ux-pro-max` برای Icon Button با label، نمودار دارای Legend متنی و حفظ زبان کارت‌های خانه | خانه خلوت‌تر می‌شود و کاربر به‌جای Market، ابتدا ترکیب پورتفوی خود را با همان زبان کارت‌های دارایی می‌بیند؛ کیف تومان هم در تصویر کلی دارایی حضور دارد | JavaScript داخل HTML Parse شد؛ تعداد سناریوها ۳۸ تأیید شد؛ Markerهای `portfolio-mix`، `portfolio-mix-hero`، `mix-card` و حذف متن راهنمای ورق‌زدن پاس شدند |
| V-027 | 10 Aug 2026 | در فلو دریافت فیزیکی، کاتالوگ شمش دو‌ستونه بود، کارت‌ها به‌جای حس محصول فیزیکی نماد فلز را برجسته می‌کردند، لیست شمش‌های انتخابی در صفحه جزئیات افقی اسکرول می‌شد و Bottom Sheet نهایی دو CTA داشت | فقط در `WF-MM-014` کارت‌های شمش به لیست عمودی تک‌ستونه با آیکون شمش/دریافت تبدیل شدند؛ CTA انتخاب شمش و تأیید جزئیات در نوار چسبیده پایین حفظ شد؛ شمش‌های انتخابی در صفحه محل/زمان دریافت عمودی شدند؛ Sheet نهایی دریافت فقط دکمه «ثبت درخواست» دارد و ارتفاع آن برای نمایش محتوای کامل بدون اسکرول تنظیم شد | بازخورد مستقیم مالک محصول در Review جزئی دریافت فیزیکی؛ D-135؛ ممیزی `ui-ux-pro-max` برای Touch target، اکشن اصلی واحد و کاهش اسکرول افقی در فلو حساس | ارائه تیمی روان‌تر، درک بهتر انتخاب شمش، کاهش ابهام CTA نهایی و حذف اسکرول افقی غیرضروری در صفحه تأیید محل دریافت | JavaScript داخل HTML با Node باندل‌شده Parse شد؛ Markerهای ساختاری تک‌ستونه شدن Grid، آیکون‌دار شدن کارت، عمودی شدن لیست انتخابی و تک‌دکمه شدن Sheet پاس شدند. Review بصری مالک محصول روی `WF-MM-014` باز است |
| V-026 | 10 Aug 2026 | فلو دریافت فیزیکی فقط انتخاب فلز و یک فرم وزن ثابت داشت؛ Multi-select، اثر دقیق کارمزد، Context تحویل، حالت ناموجود/کمبود/غیرفعال و Success/Failed ارائه‌ای وجود نداشت و قواعد طلا به‌صورت ساختاری قابل تعمیم به سه فلز نبودند | طبق D-135 فلو به Template داده‌محور طلا/نقره/مس تبدیل شد: Entry سه‌فلزی با موجودی/حداقل/قوانین، Sheet قوانین، Grid دو‌ستونه SKU با Stepper و سقف موجودی، خلاصه Sticky، جزئیات محل/زمان/کارت ملی، Rail انتخاب‌ها، Sheet نهایی مقدار فیزیکی/کارمزد/کل کسر/مانده، Success/Failed و چهار State `014A..D`. داده‌های خلوص/حداقل/SKU/کارمزد فقط Config نمونه‌اند | مشخصات صریح مالک محصول؛ D-114/D-120/D-135؛ `multi-metal-physical-delivery-flow-spec.md`؛ ممیزی `ui-ux-pro-max` برای Touch target، Disabled state، Progressive disclosure، Back و Modal | فلو قابل پرزنت و قابل استفاده برای هر سه فلز، فهم روشن تفاوت مقدار کسر و مقدار فیزیکی، بازیابی بدون بن‌بست و تغییرپذیری داده بدون بازطراحی UX | Syntax JavaScript و هشت Marker ساختاری پاس؛ ۱۵ Assertion محاسبه/Render برای طلا `۲۰۴ سوت`، نقره `۱۴۲٫۱ گرم` و مس `۱٬۰۱۰ گرم` و ۳ Assertion حفظ State در Back/Close Sheet پاس شدند. Review بصری/تعاملی مالک محصول روی `WF-MM-014/014A..D` باز است |
| V-025 | 9 Aug 2026 | Hub خدمات فهرست تک‌ستونه و کم‌تفکیک داشت؛ هشدار/سفارش و صدور/دریافت هدیه در Card مشترک بودند؛ خدمات کیف مقصد مبهمی داشت؛ اقساط فقط ساخت درخواست را نشان می‌داد و صفحات خدمات یادداشت‌های داخلی طراحی را به کاربر نمایش می‌دادند | Hub به گرید دو‌ستونه هشت‌کارت با SVG مستقل تبدیل شد؛ هشدار، سفارش قیمت، صدور هدیه و دریافت هدیه ورودی مستقیم گرفتند؛ «خدمات کیف تومان» به برداشت مستقیم تغییر کرد؛ Hub اقساط قرارداد فعال، پرداخت قسط، تاریخچه و CTA «خرید قسطی جدید» گرفت؛ پرداخت تا Loading/Result/Receipt/History متصل شد و کپی‌های `آزمایشی/Capability/OQ/Config/Legacy/تصمیم باز` از محتوای صفحات خدمات حذف شدند | بازخورد مستقیم مالک محصول؛ D-120/D-124/D-127/D-133؛ ممیزی `ui-ux-pro-max` برای SVG، گرید موبایل، فاصله ۸px و Touch target | اسکن سریع‌تر خدمات، حذف یک مرحله اضافه، نام‌گذاری روشن عملیات و امکان ارائه تیمی بدون نمایش یادداشت‌های داخلی طراحی | JavaScript syntax پاس؛ Assertion هشت Card/هشت Icon، مسیرهای مستقل هشدار/سفارش/هدیه/برداشت، Routeهای تاریخچه/پرداخت قسط و ثبت `installment-pay` پاس شد. بازکردن خودکار فایل محلی طبق سیاست امنیتی مرورگر مجاز نیست؛ Review بصری گرید و تست دستی پرداخت قسط توسط مالک محصول باز است |
| V-024 | 9 Aug 2026 | خلاصه کل و ردیف‌های جداگانه فلز در خانه، حس یک مجموعه دارایی قابل مرور را نمی‌دادند و برای رشد از ۳ به ۱۶ فلز فضای عمودی زیادی مصرف می‌کردند | به‌صورت آزمایشی بخش دارایی خانه به Rail افقی Scroll-snap تبدیل شد: کارت تیره «دارایی کل» در ابتدا و کارت‌های مستقل طلا/نقره/مس پس از آن؛ بخشی از کارت بعدی و Hint کوتاه Affordance پیمایش را نشان می‌دهند؛ کارت فلز به جزئیات متصل است و موقعیت Rail پس از Back حفظ می‌شود | درخواست مستقیم مالک محصول؛ D-105/D-113/D-120/D-133 و ممیزی `ui-ux-pro-max` برای Touch target، دسترس‌پذیری و Gesture conflict | مقیاس‌پذیری بهتر خانه تا ۱۶ فلز، مقایسه سریع‌تر دارایی‌ها و حفظ برجستگی جمع بدون بازگشت به هویت طلامحور | JavaScript syntax و Assertion ترتیب «جمع سپس فلزها»، اتصال جزئیات و حفظ Scroll پاس شد؛ کنترل خودکار فایل محلی طبق سیاست امنیتی مرورگر مجاز نبود. تأیید بصری و حس اسکرول توسط مالک محصول باز است؛ این تغییر تا آن زمان تصمیم قطعی نیست |
| V-023 | 9 Aug 2026 | Review جزئی پیش از ساخت موج بعدی مانع دیدن تجربه سرتاسری بود و موج‌های W0/W4..W8 در Prototype مشترک مسیر قابل‌کلیک نداشتند | طبق D-133 همان فایل به ۳۳ State گسترش یافت: اتصال ورود/KYC/کیف/واریز/برداشت، Hub خدمات، دریافت فیزیکی سه‌فلزی، هشدار/سفارش قیمت، اقساط و هدیه Capability-gated، معرفی دوستان و اتصال پاداش ریالی به Tab واریز؛ همه مسیرها به Preview، Result، Receipt یا History متصل شدند | درخواست صریح مالک محصول برای ساخت همه موج‌ها پیش از اصلاح جزئی؛ فلوهای رسمی `FL-PHYSICAL/TARGET/INSTALLMENT/GIFT/REFERRAL` و D-120/D-127/D-133 | مشاهده کل اپ در یک ارائه و امکان اصلاح موج‌به‌موج بدون بن‌بست یا ادعای زودهنگام عرضه | W0 و W4..W8 کلیک‌به‌کلیک تا نتیجه تست شدند؛ هر ۳۳ State بدون Runtime error Render شد، Touch target حداقل ۴۴، Responsive ۳۲۰/۳۶۰/۳۷۵/۳۹۰/Landscape/Desktop و Reduced Motion پاس شد. پوشش ساخت همه موج‌ها کامل و بلوغ هر موج ۷۵٪؛ Review جزئی باز است |
| V-022 | 9 Aug 2026 | Prototype مشترک Stateها را جداگانه نمایش می‌داد اما کلیک داخل Sheet به‌علت توقف Event کار نمی‌کرد، Scrim بسته نمی‌شد، Back فقط نام صفحه را نگه می‌داشت، فیلتر تاریخ اثری روی نتایج نداشت و CTA معامله بن‌بست نمایشی بود | Event model Sheet اصلاح شد؛ بستن با Scrim/×/Escape و بازگشت به State پایه، Snapshot کامل Navigation و حفظ فرم/فیلتر/Scroll، Draft و Apply فیلترها، فیلتر واقعی تاریخ، انتخاب‌گرهای معامله/نمودار/تاریخچه، Services tab و Sheet حساب/اعلان متصل شدند؛ معامله از ورودی تا Preview، Loading، رسید و تاریخچه قابل اجرا شد و Toast دیگر لمس را مسدود نمی‌کند | بازخورد مستقیم مالک محصول هنگام تلاش برای ارائه؛ D-120/D-121/D-123/D-128؛ ممیزی `ui-ux-pro-max` | تبدیل Deck فریم‌ها به Prototype پیوسته و قابل ارائه بدون از‌دست‌رفتن State یا کنترل‌های ظاهراً غیرفعال | دو سناریوی مرورگری Click-by-click پاس شدند: مسیر خانه→بازار→جزئیات→معامله→Confirm تغییر فلز→Preview→Loading→رسید→تاریخچه، فیلترهای U2، Tabهای ریالی، Services/Notifications، Stress شانزده‌فلزی و نمودار. عرض‌های ۳۲۰/۳۶۰/۳۷۵/۳۹۰، Landscape و Desktop، Touch target حداقل ۴۴ و Reduced Motion پاس شدند؛ Review ارائه‌ای مالک محصول باز است |
| V-021 | 9 Aug 2026 | موج MM-W2 در Prototype مشترک حضور نداشت و ارتباط انتخاب فلز، Quote فعال و نمودار مستقل فقط در قرارداد اسنادی ثبت شده بود | پنج State `WF-MM-008..012` به همان فایل اضافه شد: معامله با طلای پیش‌فرض و ورودی خالی، Sheet سه‌فلزی، Sheet شانزده‌فلزی با Search و Assetهای غیرفعال، Confirm پاک‌شدن مبلغ/وزن/Quote هنگام تعویض و نمودار تک‌فلز با `chartAssetId` مستقل و CTA صریح انتقال Context | D-121/D-123، بسته `WF-MM-APP-001` و Reuse ماشین‌حساب/معامله موج ۱ | سنجش مدل B بدون ساخت فرم جدا برای هر فلز و جلوگیری از معامله یا نمودار روی Asset اشتباه | JavaScript syntax و Assertionهای پنج شناسه، طلای پیش‌فرض، Selector ۳/۱۶، Confirm Quote و استقلال Chart پاس شدند. موج ۷۵٪ و هسته ۲۰خروجی ۷۱٪؛ Review مالک محصول و QA واقعی باز است |
| V-020 | 9 Aug 2026 | `MM-W1/MM-W3` فقط Inventory و Baseline تک‌فلزی داشتند و خانه/Market/جزئیات سه فلز، Stress state شانزده‌فلزی، U2 یکپارچه و رسید چندفلزی در یک Prototype قابل بررسی نبودند | Prototype تعاملی ۱۳صفحه‌ای با شل قبلی ساخته شد: خانه عادی/نرخ ناقص/اختلال، Market سه/شانزده، Template جزئیات طلا/نقره/مس با دقت ۳/۲/۰، Zero/Guest، تاریخچه ۱۲Tab، Selector مستقل فلز، حذف Selector در واریز/برداشت، Receipt و Back/Deep Link | D-105 تا D-131؛ `multi-metal-full-app-wireframe-readiness-pack.md` و وایرفریم‌های موج ۱/۲ قبلی | امکان Review کل ساختار دو موج بدون طراحی مجدد بخش‌های ریالی و بدون ساخت سه اپ موازی | Syntax و Assertionهای ۱۳ شناسه، نبود CTA انتقال، مرز واریز/برداشت و بازسازی رسید پاس شد. بازکردن خودکار فایل محلی به‌دلیل محدودیت امنیتی ابزار ممکن نشد؛ Review مالک محصول و QA واقعی عرض‌ها باز است. Gate بخش مشترک/تاریخچه همچنان `Sufficient to prototype` |
| V-019 | 9 Aug 2026 | جدول موج‌های قبلی با ساختار چندفلزی و Inventory بیست‌صفحه‌ای هم‌راستا نبود و مقدار کار قابل Reuse را از کار واقعاً ساخته‌شده جدا نمی‌کرد | موج‌های `MM-W0..MM-W8` تعریف، چهار موج هسته به شناسه‌های `WF-MM` نگاشت و قرارداد امتیاز ۰/۲۵/۵۰/۷۵/۱۰۰ برای بلوغ وایرفریم ثبت شد؛ App Shell/Auth/Deposit و گزینه U2 به‌عنوان شاهد Reuse ممیزی شدند | D-120 تا D-131؛ فایل‌های `wave-1-app-shell-wireframe.html`، `wave-1-auth-kyc-wireframe.html`، `wave-1-interactive-wireframe.html` و `wave-2-deposit-wireframe.html` | گزارش پیشرفت قابل تکرار، جلوگیری از شمردن آمادگی اسنادی به‌عنوان ساخت و حفظ سرمایه طراحی قبلی بدون فرض تکمیل چندفلزی | محاسبه ۲۰ خروجی بازآزمایی شد: `MM-W1=20%`، `MM-W2=15%`، `MM-W3=50%`، `MM-W4=37.5%≈38%` و مجموع وزنی دقیقاً ۲۵٪؛ برنامه‌ریزی این تسک `Complete` و Gate ساخت بخش مشترک/تاریخچه `Sufficient to prototype` است |
| V-018 | 8 Aug 2026 | موج ۲ طبق برنامه قدیمی خرید/فروش/رسید را دوباره تکرار می‌کرد و واریز هنوز Prototype مستقل، مقایسه روش، Pending پایدار و اتصال به کمبود خرید نداشت | موج ۲ با D-103 به واریز بازتنظیم شد؛ بسته آمادگی مستقل ساخته و Prototype مبلغ، پنج روش، Gate نقش‌ها، تحویل/ثبت، شارژشده/درحال‌تطبیق/ناموفق/برگشتی، رسید، قطعی سراسری و Concept غیرنهایی مسیر کلان پیاده شد؛ CTA واریز App Shell و کمبود خرید به آن متصل و پس از شارژ خرید با مبلغ/موجودی حفظ‌شده Resume می‌شود | FL-DEPOSIT-001؛ D-035/D-044/D-058/D-067/D-068/D-088/D-103؛ BR-010/027–032/049–052/081–082؛ ممیزی `ui-ux-pro-max` | انتخاب روش متناسب‌تر، کاهش واریز تکراری در Pending، پیگیری روشن و حذف دوباره‌کاری موج ۱ | JavaScript syntax و Assertions پاس؛ مسیر درگاه/شارژ، کارت‌به‌کارت/تطبیق، Gate KYC، مبلغ کلان و اتصال دوطرفه خرید در ۳۲۰/۳۹۰px با DOM و Screenshot بررسی شد؛ Review مالک محصول باز است |
| V-017 | 8 Aug 2026 | مبلغ اصلی، کارمزد و پرداخت نهایی خرید باهم جمع نمی‌شدند؛ سه عنوان هم‌معنا روی Preview تکرار می‌شد؛ CTA در عرض ۳۲۰px نیمه‌نمایان بود؛ Deep Link احراز از App Shell صفحه ورود را باز می‌کرد | پرداخت نهایی به‌عنوان مبلغ شامل کارمزد تثبیت و مبلغ اصلی از آن کسر شد؛ عنوان‌ها به Header نوع معامله + «بررسی نهایی» + CTA تأیید ساده شدند؛ CTA اصلی Preview/Result به نوار ثابت پایین منتقل شد؛ `#kyc` مستقیماً فرم احراز کاربر واردشده را باز می‌کند | Review مستقیم مالک محصول و بازبینی `ui-ux-pro-max` — D-101 | فهم بی‌ابهام جمع مالی، دسترسی کامل به CTA در موبایل کوچک و حذف Loop ورود برای کاربر احرازنشده | JavaScript syntax و Assertions پاس؛ سناریوی خرید و Deep Link KYC روی HTTP در عرض ۳۲۰px بررسی و نسخه توسط مالک محصول تأیید شد |
| V-016 | 8 Aug 2026 | اقدام برداشت داخل Card کیف جهت مطلوب نداشت و Prototype معامله پس از ماشین‌حساب متوقف می‌شد؛ Preview، Pending، نتیجه، اثر روی موجودی و Recovery قابل آزمون نبودند | «برداشت از کیف» به سمت چپ Card کیف منتقل شد؛ بسته آمادگی خریدوفروش ساخته و مسیرهای Buy/Sell تا Preview مستقل، Gate تأیید، Pending ضد Submit تکراری، رسید موفق/ناموفق، به‌روزرسانی دارایی و درج در تاریخچه پیاده شد؛ کمبود تومان/طلا Recovery نوع‌محور دارد | درخواست صریح مالک محصول؛ FL-BUY-001، FL-SELL-001، State Matrix و ممیزی `ui-ux-pro-max` — D-100 | امکان Review واقعی معامله، کاهش ثبت ناخواسته/تکراری، فهم بهتر اثر مالی و بازیابی بدون بن‌بست | JavaScript syntax پاس و قراردادهای ایستا بررسی شد؛ Review بصری/تعاملی مالک محصول باز است |
| V-015 | 8 Aug 2026 | دو Card صندوق طلا/کیف پول کنار هم برای اعداد و توضیح معادل فضای محدودی داشتند و ورودی تاریخچه پس از چهار خدمت دیده می‌شد | Cardهای دارایی در یک ستون با ترتیب صندوق طلا سپس کیف پول قرار گرفتند؛ Card متمایز تاریخچه به ابتدای بلوک و بالای چهار خدمت منتقل شد | Review مستقیم مالک محصول — D-099 | خوانایی بهتر دارایی‌ها و کشف زودتر تاریخچه عملیات | JavaScript syntax و ممیزی ایستا پاس؛ Review بصری/تعاملی مالک محصول باز است |
| V-014 | 8 Aug 2026 | تاریخچه همه عملیات داخل صفحه دارایی از نظر دامنه گمراه‌کننده و دسترسی آن ضعیف بود؛ Card دارایی عنوان/نام و Hint اضافی داشت؛ CTAها بیش‌ازحد عمودی بودند؛ معامله قبل از ورود مقدار موجودی و قوانین را نشان نمی‌داد و CTA خالی به فیلد می‌چسبید | تاریخچه به صفحه مستقل با فیلترهای تراکنش/درخواست منتقل و Card متمایز آن کنار چهار خدمت خانه اضافه شد؛ تاریخچه از دارایی حذف شد؛ Card خانه بدون عنوان و با «صندوق طلا/کیف پول»، معادل ریالی و CTAهای افقی فشرده اصلاح شد؛ معامله موجودی Contextual، لینک Bottom Sheet قوانین قبل از فیلد، Label «مقدار طلا» و فاصله پایدار CTA گرفت | تأیید و بازخورد مستقیم مالک محصول — D-098؛ ممیزی پیش‌فرض `ui-ux-pro-max` برای سلسله‌مراتب، Touch target و Progressive disclosure | یافتن یک‌اقدامی تاریخچه، جداسازی روشن دارایی از درخواست خدمت، فهم بهتر قدرت خرید/فروش و جلوگیری از CTA چسبیده در حالت خالی | JavaScript syntax و ممیزی ایستا پاس؛ کنترل خودکار بصری فایل محلی به‌علت سیاست امنیتی مرورگر انجام نشد و Review بصری/تعاملی مالک محصول باز است |
| V-013 | 3 Aug 2026 | نمودار پیش از محتوای شخصی قرار داشت و بازه/نقطه تعاملی نداشت؛ Card دارایی CTAهای اضافی داشت؛ Banner تک‌اسلایدی و خدمات خانه کوچک بودند؛ معامله در حالت اولیه اطلاعات و مقدار نمونه اضافی نشان می‌داد | نمودار به انتهای خانه منتقل و به Tabهای روزانه/هفتگی/ماهانه با Tooltip در Hover/Focus/Tap مجهز شد؛ دارایی افقی با خرید/واریز و لمس کیف برای برداشت ساخته شد؛ Slider سه‌اسلایدی Placeholder و Cardهای خدمات هم‌اندازه Hub شدند؛ Lead و نرخ/موجودی معامله حذف و ورودی/نتیجه Progressive شدند | Review مستقیم مالک محصول — D-097 | تمرکز بیشتر روی وضعیت شخصی و خدمات، نمودار قابل کاوش و حالت شروع خلوت‌تر معامله | JavaScript syntax و ممیزی ایستا پاس؛ Review بصری/تعاملی مالک محصول باز است |
| V-012 | 3 Aug 2026 | «ارزش کل» مبنای نامشخص داشت، طلا و کیف را بی‌دلیل تجمیع می‌کرد، برداشت کنار موجودی نقدی نبود، میان‌بر فروش تکراری بود و خدمات/فیلتر تراکنش کامل نبودند | عدد تجمیعی حذف و طلا/کیف مستقل شدند؛ برداشت فقط برای موجودی مثبت اضافه شد؛ میان‌برها به دریافت شمش/سفارش هدف/هشدار/اقساط تغییر کردند؛ خدمات در پنج دسته مستند تکمیل شد؛ فیلتر چهارگروهی + Bottom Sheet، رسید و اطلاعات Trade غنی‌تر شدند؛ حالت کیف صفر/مثبت به کنترل Prototype اضافه شد | تأیید مستقیم نیازسنجی توسط مالک محصول — D-096 | فهم دقیق نوع دارایی، کشف طبیعی برداشت، حذف تکرار و پوشش بهتر خدمات/تراکنش بدون اختراع قابلیت | JavaScript syntax و ممیزی ایستا پاس؛ کنترل مرورگر فایل محلی را طبق سیاست امنیتی باز نکرد و Review بصری/تعاملی مالک محصول باز است |
| V-011 | 3 Aug 2026 | خانه نرخ را دیر و با عنوان/لینک/Timestamp تکراری نشان می‌داد؛ Card دارایی CTA اضافه داشت؛ Banner و اقدام‌های منتخب جای روشن نداشتند؛ آخرین فعالیت خانه را شلوغ و تب معامله را نمودار/اقدام ثانویه سنگین می‌کرد | نرخ خرید و نمودار به بالای خانه منتقل شد؛ Card «دارایی من» با Summary قابل لمس و خرید/فروش/واریز ساخته شد؛ Banner rail و چهار اقدام محدود اضافه و آخرین فعالیت حذف شد؛ صفحه دارایی با ارزش کل، طلا، کیف پول، فروش/واریز و تراکنش‌ها یکپارچه شد؛ نمودار و اقدام‌های بیشتر از معامله حذف شدند | Review مستقیم مالک محصول — D-095 | سلسله‌مراتب روشن‌تر، کاهش تکرار و مسیر متمرکزتر برای دارایی و رسید | Syntax و قراردادهای ایستا پاس؛ Review بصری/تعاملی مالک محصول باز است |
| V-010 | 3 Aug 2026 | مدل ناوبری سه‌تبی تأیید شده بود اما فایل مستقل App Shell و پوشش نقش‌ها/نرخ/مقصدهای داخلی وجود نداشت | Prototype مستقل خانه/معامله/خدمات ساخته شد؛ دارایی، تراکنش، رسید و حساب مقصد داخلی شدند؛ Stateهای مهمان، احرازنشده، زیر ۱۸، محدود و نرخ نامعتبر اضافه شدند؛ ورودی ماشین‌حساب بدون Render مجدد و با حفظ Focus به‌روزرسانی می‌شود | D-094، بسته آمادگی App Shell و ممیزی `ui-ux-pro-max` | سنجش واقعی ناوبری، کاهش ابهام جای دارایی/تراکنش و جلوگیری از گیرکردن Stateهای محدود | Syntax JavaScript و قراردادهای ایستا پاس؛ کنترل خودکار بصری فایل محلی به‌علت سیاست امنیتی مرورگر انجام نشد و Review مالک محصول باز است |
| V-009 | 1 Aug 2026 | Toast ساخت حساب در Transition مستقیم به KYC اضافه بود؛ Requirement رمز یک متن ثابت بود؛ Blur Picker کل آیتم‌های بالا و پایین را بیش‌ازحد محو می‌کرد | Toast حذف شد؛ سه شرط رمز به چک‌لیست زنده با Check مستقل تبدیل شد؛ ناحیه Blur هر لبه از ۶۸ به ۴۴px و شدت از ۲٫۵ به ۱٫۵px کاهش یافت | بازخورد مستقیم مالک محصول | Transition بی‌حواس‌پرتی، فهم سریع پیشرفت رمز و حفظ خوانایی گزینه‌های نزدیک تاریخ | Syntax و قراردادهای ایستا پاس؛ Review لمسی مالک محصول باز |
| V-008 | 1 Aug 2026 | سیاست رمز و Routing ثبت‌نام جدید در Review نیازمند اصلاح بود؛ کنترل متنی Show/Hide و قاب Picker نیز تمیز نبود | رمز به حداقل ۸ با حرف انگلیسی/عدد تغییر کرد؛ سقف فنی از متن UI حذف و کنترل با SVG چشم جایگزین شد؛ Signup پس از ساخت رمز مستقیم KYC را باز می‌کند؛ پیام سن کوتاه شد؛ قاب انتخاب Picker حذف و Blur/Fade بالا/پایین اضافه شد | تأیید صریح مالک محصول — D-093؛ ممیزی `ui-ux-pro-max` برای Icon، Touch target و Error نزدیک فیلد | کاهش متن اضافی، مسیر سریع‌تر Signup و Picker نزدیک‌تر به الگوی موبایل | بررسی نحوی و قراردادهای ایستا انجام شد؛ Review لمسی مالک محصول باز |
| V-007 | 1 Aug 2026 | هزینه OTP در مقیاس کاربران، Routing وابسته به مبدأ و فرض ممنوعیت حساب زیر ۱۸ سال با تصمیم تازه محصول ناسازگار شد | ورود روزمره به موبایل/رمز تغییر کرد؛ کاربر جدید، قدیمی فاقد رمز و بازیابی با OTP و ساخت رمز ادامه می‌دهند؛ رمز ۱۰–۶۴ کاراکتر است؛ همه Authها به خانه می‌روند؛ KYC خودکار نیست؛ زیر ۱۸ سال حساب دارد اما عملیات مالی قفل است؛ نشست سه ساعت بی‌فعالیتی و Resend برابر ۶۰ ثانیه شد | تصمیم تأییدشده مالک محصول — D-092؛ راهنمای Twilio/Firebase برای فاصله Resend | کاهش هزینه OTP، مسیر قابل پیش‌بینی و پوشش مهاجرت کاربران قدیمی بدون تحمیل KYC | Syntax JavaScript موفق؛ Review تعاملی/بصری مالک محصول باز |
| V-006 | 28 Jul 2026 | اصلاحات جزءبه‌جزء Auth/KYC نشان داد Prototype پیش از ممیزی کامل قواعد، eligibility، قرارداد رفتاری/Component و حالت‌های لبه ساخته شده و مالک محصول عملاً QA جزئیات پایه را انجام می‌دهد | D-091 و قالب اجباری بسته آمادگی برای همه موج‌ها ثبت شد؛ ساخت فقط پس از `Sufficient to prototype` مجاز است؛ Auth/KYC به `Needs revision` برگشت و سن/اهلیت در OQ-047 ثبت شد | بازخورد و تأیید صریح مالک محصول پس از ممیزی روند | انتقال QA استاندارد و کشف پیش‌دستانه به طراح/دستیار، کاهش چرخه Patch و تمرکز مالک محصول بر تصمیم بیزینسی | ثبت اسناد کامل؛ بازآزمایی روش با بسته آمادگی Auth/KYC انجام می‌شود |
| V-005 | 28 Jul 2026 | Bottom Sheet تاریخ از Selectهای معمولی استفاده می‌کرد و رفتار Wheel استاندارد موبایل نداشت؛ خروج هم به دکمه انصراف بصری وابسته بود | روز/ماه/سال به سه Wheel مستقل Scrollable با Scroll Snap و ردیف انتخاب مرکزی تبدیل شدند؛ انصراف بصری حذف و بستن با Scrim، Drag-down Handle، Escape و کنترل Screen Reader پیاده شد؛ تاریخ فقط با CTA تأیید ذخیره می‌شود | بازخورد مستقیم مالک محصول در Review Bottom Sheet | تعامل آشناتر موبایل، کاهش کنترل اضافه و حفظ خروج دسترس‌پذیر | بررسی نحوی موفق؛ Review لمسی مالک محصول باز |
| V-004 | 28 Jul 2026 | Home نمونه پس از OTP یک توقف غیرضروری در Review پیوسته Auth/KYC بود | OTP موفق مستقیماً فرم KYC را باز می‌کند و Feedback موفقیت ورود به‌صورت Toast روی همان مقصد دیده می‌شود؛ Home فقط مقصد پس از تکمیل/تعویق KYC باقی ماند | بازخورد مستقیم مالک محصول همراه اسکرین‌شات | حذف یک گام و حفظ Feedback بدون Screen واسط | بررسی نحوی موفق؛ Review مالک محصول باز |
| V-003 | 28 Jul 2026 | کپی ورود Context خرید را بی‌دلیل فرض می‌کرد؛ OTP دستی Submit می‌شد، در خطا پاک نمی‌شد و تایمر اعتبار کامل نداشت؛ تاریخ تولد Text Input بود؛ خروج KYC و بازخورد موفقیت روشن نبود | ورود عمومی شد؛ OTP با رقم ششم Auto-submit، در خطا/انقضا پاک و دارای تایمر ۳دقیقه/Resend شد؛ Success به Toast گذرا و Home نمونه متصل شد؛ KYC خروج «بعداً» گرفت؛ تاریخ تولد به Bottom Sheet شمسی روز/ماه/سال تبدیل شد؛ Gate اجباری Review رفتاری موبایل به برنامه وایرفریم اضافه شد | بازخورد مستقیم مالک محصول در دور دوم Review فایل مستقل Auth/KYC | کاهش نیاز به QA دستی مالک محصول و پوشش پیش‌دستانه رفتارهای استاندارد موبایل | بررسی نحوی موفق؛ Review بصری و تعاملی مالک محصول باز |
| V-002 | 28 Jul 2026 | OTP تک‌فیلدی دیده می‌شد، عنوان‌ها و راهنماها تکراری بودند، و نتیجه ورود/Gate احراز به Screenهای میانی غیرضروری تبدیل شده بودند | OTP به شش خانه بصری با یک Input معنایی تبدیل شد؛ متن‌های بدیهی حذف شدند؛ OTP در سناریوی خرید مستقیماً به فرم KYC و KYC مستقیماً به مقصد برمی‌گردد؛ Back احراز حذف شد | بازخورد مستقیم مالک محصول در Review فایل مستقل Auth/KYC | کاهش گام و بار شناختی، حفظ Autofill و روشن‌شدن مقصد Back | بررسی نحوی موفق؛ Review بصری مالک محصول باز |
| V-001 | 28 Jul 2026 | Prototype جامع موج ۱ فلوهای متعدد را در یک فایل و قاب شلوغ جمع کرده بود؛ فونت، مقیاس موبایل، فاصله‌ها و Back/Close نیازمند اصلاح بودند | Auth/KYC در `artifacts/wave-1-auth-kyc-wireframe.html` مستقل شد؛ Yekan Bakh 3 محلی، کنترل‌های ۴۸/۵۲px، ریتم ۴/۸px، فرم تک‌ستونه و ناوبری حداقلی اعمال شد | بازخورد مستقیم مالک محصول در Review داخلی؛ تطبیق با `FL-AUTH-001` و `FL-KYC-001` | تمرکز Review روی یک فلو، کاهش شلوغی، لمس‌پذیری بهتر و تشخیص روشن‌تر مسیر برگشت | Review مالک محصول باز؛ Syntax و ساختار HTML بررسی شد |
| V-XXX |  |  |  |  |  |  |
