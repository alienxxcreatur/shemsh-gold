# مرحله ۶ — وایرفریم، پروتوتایپ و تست

## هدف

اعتبارسنجی ساختار، فهم، ترتیب و بازیابی از خطا پیش از سرمایه‌گذاری روی UI نهایی.

## خروجی‌ها

- `flow-readiness-pack-template.md`
- `multi-metal-full-app-wireframe-readiness-pack.md`
- `app-shell-product-needs-assessment.md`
- `wireframe-plan.md`
- `usability-test-plan.md`
- `findings-log.md`
- `iteration-log.md`
- `receipt-result-structure-contract-v0.1.md`
- `operation-detail-structure-contract-v0.2.md`
- `pre-binance-wireframe-readiness-plan-v0.1.md`
- `home-zero-portfolio-mobbin-pattern-audit-2026-08-18.md`
- `artifacts/` برای نمونه‌های قابل اجرا یا خروجی‌های وابسته

## مراجع ارائه و اشتراک وایرفریم

- `Observed fact` — مرجع اصلی بیشتر فلوهای محصول: `artifacts/multi-metal-waves-1-and-3-wireframe.html`؛ شامل فلوهای چندفلزی، فلو سبز و سناریوهای متصل قابل مرور.
- `Observed fact` — مرجع مستقل ورود، ثبت‌نام و احراز هویت: `artifacts/wave-1-auth-kyc-wireframe.html`.
- `Design assumption` — برای Review مدیریتی، این دو فایل از یک آدرس وب ثابت ارائه شوند؛ انتشار مستقیماً از نسخه ثبت‌شده در مخزن انجام شود تا هر Push تأییدشده، همان آدرس را به نسخه جدید به‌روزرسانی کند.
- `Observed fact` — فایل اول مرجع اصلی اپ است و طبق D-200 همان فایل تخصصی Auth/KYC را در سناریوهای ورود و احراز اجرا می‌کند؛ کپی ساده یا موازی این فلو در فایل اصلی نگهداری نمی‌شود. طبق D-201 نمای کلی نیز هفت State واقعی همین فایل تخصصی را نشان می‌دهد و پنج کارت عمومی Auth/KYC قبلی حذف شده‌اند. فایل‌های Candidate یا تاریخی جایگزین این دو مرجع ارائه نیستند.
- `Observed fact` — خروجی اشتراک با `scripts/build-wireframe-share.mjs` ساخته می‌شود؛ خروجی دو HTML، سیاست مشترک رمز `password-policy.js`، فونت وابسته، صفحه انتخاب، Header جلوگیری از Index شدن و Worker محدود `/api/live-prices` را در `dist-wireframes/` قرار می‌دهد. فایل خالی `.assetsignore` نیز انتشار عمدی `_worker.js` را برای Wrangler صریح می‌کند. برای Review مستقیم فایل `file://`، Helper محلی با `node scripts/serve-wireframe-live-prices.mjs` اجرا می‌شود؛ نبود Helper فقط نرخ‌ها را به Fallback نمونه برمی‌گرداند و صفحه را نمی‌شکند.

## Gate 6

- [ ] هر فلو پیش از ساخت، بسته آمادگی با وضعیت `Sufficient to prototype` داشته باشد.
- [ ] فلوهای Critical پروتوتایپ قابل تست داشته باشند.
- [ ] سناریوها شامل اعتماد و خطا باشند، نه فقط مسیر موفق.
- [ ] حداقل کاربران نماینده مخاطب اصلی تست شده باشند.
- [ ] یافته‌های Critical و High حل یا آگاهانه پذیرفته شوند.
- [ ] نسخه اصلاح‌شده دوباره بررسی شود.

## وضعیت انتقال موج ۱

> یادداشت D-105: خروجی موج ۱ حذف نشده، اما App Shell، دارایی، معامله، نمودار و رسیدهای فلزمحور منبع نهایی پلتفرم چندفلزی نیستند و تا Impact review وضعیت `Needs revision` دارند. Auth/KYC و الگوهای عمومی/ریالی قابل استفاده مجدد باقی می‌مانند.

- موج ۱: App Shell، ورود/ثبت‌نام، KYC، قیمت/ماشین‌حساب و Prototype خریدوفروش
- سند پشتیبان نیازسنجی: `app-shell-product-needs-assessment.md`
- بسته آمادگی ساختاری: `app-shell-readiness-pack.md`
- بسته آمادگی خریدوفروش: `trade-flow-readiness-pack.md`
- Prototype فعال App Shell: `artifacts/wave-1-app-shell-wireframe.html`
- Prototype فعال Auth/KYC: `artifacts/wave-1-auth-kyc-wireframe.html`
- Prototype جامع تاریخی: `artifacts/wave-1-interactive-wireframe.html`
- خروجی موج ۱ با D-102 برای عبور موج‌محور بسته و ورودی رسمی مرحله ۷ شده است.
- یافته‌ها و سؤال‌های باز مانند OQ-048 حذف نشده‌اند و پیش از UI نهایی یا `Stable` شدن Pattern متأثر باید پیگیری شوند.

وضعیت Gate 6: با D-105 برای بخش‌های فلزمحور باز شده است. App Shell سه‌تبی و Prototype D-101 به‌عنوان شواهد و Candidate حفظ می‌شوند، اما باید حداقل با دو فلز دارای تفاوت واقعی بازآزمایی شوند. ساخت تازه موج ۳ و Review نهایی موج ۲ تا تصمیم Release چندفلزی متوقف‌اند؛ بسته آمادگی D-091 همچنان اجباری است.

## جریان فعال وایرفریم کل اپ چندفلزی

- بسته آمادگی: `multi-metal-full-app-wireframe-readiness-pack.md`
- دامنه: خانه، بازار داخلی، جزئیات فلز، معامله، نمودار، خدمات، تاریخچه و رسید در حالت ۳ تا ۱۶ Asset
- قراردادهای بسته: D-120 تا D-123؛ ورود مستقیم معامله با طلا، Context صریح مقدم
- وضعیت Gate: تاریخچه و بخش‌های مشترک با D-128 `Sufficient to prototype` هستند؛ Feature-specific زیر D-127/OQ-054 خنثی می‌ماند
- مسیر قطعی D-130: ساخت وایرفریم ساده بخش‌های مشترک/تاریخچه آغاز می‌شود و Review OQ-054 موازی پیش می‌رود؛ هیچ صفحه Feature-specific به یک فلز هاردکد نمی‌شود
- موج‌بندی اجرایی D-131: `MM-W0` اتصال Reuse، `MM-W1` خانه/بازار/جزئیات، `MM-W2` معامله/نمودار، `MM-W3` تاریخچه/رسید، `MM-W4` خدمات/دریافت و `MM-W5..MM-W8` فیچرهای Capability-gated
- پیشرفت ساخت هسته ۲۰ صفحه‌ای: **۷۵٪**؛ هر چهار موج `MM-W1..MM-W4` در بلوغ ۷۵٪ قرار دارند
- پوشش افقی: تمام `MM-W0..MM-W8` با ۲۸ خروجی هدف و پنج State اتصال ساخته شده‌اند؛ بلوغ هر موج ۷۵٪ و Review جزئی باز است — D-133
- Prototype فعال: `artifacts/multi-metal-waves-1-and-3-wireframe.html` با حفظ نام تاریخی و ۱۰۱ صفحه/حالت قابل مرور برای Auth/KYC، پورتفوی، واریز، برداشت، معامله، خدمات، دریافت فیزیکی، قیمت، اقساط، هدیه، معرفی، حساب کاربری، تاریخچه، رسید و پشتیبانی. اقساط طبق D-162 یک مقصد صفحه‌ای واحد با دو State مستقل در ستون سناریو دارد: `WF-MM-023A` قرارداد فعال را با مدیریت/پرداخت و `WF-MM-023B` نبود قرارداد فعال را با فرم شروع خرید جدید نشان می‌دهد؛ صفحه Setup جدا حذف شده اما Preview/Result حفظ است. طبق D-194/D-195 نمای روزمره قرارداد فعال خلاصه/پیشرفت و قسط بعدی را در یک Card دارد؛ جزئیات با `Chevron Right` از ردیف دارایی باز می‌شود و برنامه اقساط فقط داخل جزئیات قرارداد نمایش داده می‌شود. کیف تومان طبق D-142/D-145 صفحه مستقل ندارد: لمس کل کارت خانه Bottom Sheet مینیمال واریز/برداشت را باز می‌کند؛ واریز با درگاه آنلاین به‌عنوان مسیر اصلی، انتخاب کارت مبدأ و State بدون کارت/چندکارت آغاز می‌شود. طبق D-153/D-155/D-210، Trigger بدون عنوان سکشن «روش‌های دیگر واریز» حتی بدون مبلغ، کارت‌به‌کارت، شبا یا حساب‌به‌حساب را باز می‌کند و هر انتخاب به صفحه اطلاعاتی مستقل با Back و حفظ مبلغ/مبدأ می‌رود؛ Card مقصد خنثی و متراکم است و صفحه فقط تب پل/پایا، شناسه/توضیحات الزامی، Copy و SLA نوع‌محور دارد، نه CTA ثبت/ادامه. CTA درگاه Preview/تأیید داخلی را دور می‌زند و پس از Loading انتقال مستقیماً به موفق/ناموفق/نامشخص برمی‌گردد. طبق D-156، تمام Contextهای انتخاب کارت در این مسیرها و برداشت از `bankCardSelector` و `bankCardPicker` مشترک تغذیه می‌شوند. درنتیجه پنج قابلیت D-035 حفظ می‌شوند و داده‌های بانکی Prototype نمونه‌اند. خانه طبق D-147/D-149 CTA فشرده «خرید و فروش» را نزدیک Rail و پیش از کیف دارد؛ عنوان/آیکن جداگانه دارایی حذف و کارت دارایی کل ورودی صفحه ترکیب است. طبق D-150 موجودی دارایی کل، هر فلز و کیف تومان در ورود اولیه مخفی است و هر کارت چشم مستقل نزدیک محدوده موجودی/ارزش دارد؛ زیر چهار Shortcut خانه Slider بنر کمپین قرار دارد. طبق D-148 سود/زیان روبه‌روی عنوان کارت با Arrow شکسته است؛ چهار Shortcut تاریخچه/اقساط/دریافت/سفارش در گرید مشترک‌اند. Hub خدمات طبق D-182/D-184 عنوان تکراری ندارد و ۹ Tile مربع با آیکن بدون قاب را در گرید ۳×۳ نشان می‌دهد؛ «هدیه» ورودی واحد Hub صدور/دریافت است و تعداد ۱۰ تا ۱۲ خدمت خودکار به گرید ۴×۳ بدون Scroll تبدیل می‌شود. حساب با D-137 صفحه کامل است و از Avatar باز می‌شود؛ تاریخچه داخل آن تکرار نمی‌شود. پوشش Stateهای D-193 با Renderer مرکزی و متن اثر مالی/پیگیری/Recovery اختصاصی به این مسیرها افزوده شده است
- ستون Review دسکتاپ همین Prototype اکنون ۱۰۱ صفحه/حالت را در ۱۴ Accordion فلومحور نمایش می‌دهد؛ هر گروه تعداد صفحه‌ها، باز/بستن مستقل، کنترل باز/بستن همه و بازشدن خودکار گروه صفحه فعال دارد. Stateهای ضروری هر عملیات از Entry/Processing تا موفق، ناموفق، نامشخص، لغو، انقضا یا برگشت—در صورت نیاز همان فلو—مستقل‌اند. این دسته‌بندی فقط ابزار مرور وایرفریم است و روی شل موبایل اثر ندارد — D-193/D-210
- `Observed fact` — همان فایل مرجع یک حالت دوم «نمای کلی همه نماها» دارد: Inventory خودکشف فعلی ۱۴۷ Snapshot یکتا شامل ۷۵ Page، ۵۵ State و ۱۷ Overlay را در ۱۴ گروه روی Canvas نقطه‌ای می‌سازد. Zoom کنترل‌شده، `Ctrl/Cmd + Wheel`، Pan، Fit-all، `Escape`، Deep link `?overview=1` و Drill-down تعاملی پشتیبانی می‌شوند. Routeهای صریح تازه با Reload از Renderer کشف می‌شوند؛ خانواده پویا یا View وابسته فقط Context کوچک Review می‌خواهد. Screenshot یا فایل موازی نگهداری نمی‌شود — D-199/D-203/D-205/D-210 / V-133/V-134/V-138/V-140/V-144
- `Observed fact` — ممیزی `full-app-wireframe-cleanup-audit-2026-08-18.md` در زمان تهیه نشان داد ۳۱ مقصد تمام‌صفحه و ۱۴ Overlay مستقل از Catalog ۱۰۲ سناریویی جا مانده‌اند. مرحله‌های ۱ تا ۴ کامل‌اند: Copy داخلی پاک، Inventory خودکشف، Resultهای Canonical و CTAهای بدون Toast نمایشی اجرا شده‌اند. مرحله ۵ فقط وابستگی‌های Business-gated را نگه می‌دارد.
- `Observed fact` — مرحله ۳ ممیزی در V-139 کامل شد: نتیجه واقعی و State عمومی معامله، دریافت، اقساط، هدیه و Feature Result از مدل و Component Canonical مشترک استفاده می‌کنند. Receipt/Asset واقعی بر نمونه Review مقدم است، Context مستقیم اقساط با نقره همگام است و جزئیات تخصصی بدون ساخت قانون تازه زیر خلاصه مشترک باقی می‌مانند — D-204.
- `Observed fact` — با D-207/V-141، اشتراک منبع داده D-204 حفظ اما Composition Result و Receipt جدا شد. Result وضعیت/اثر/راه ادامه را خلاصه می‌کند؛ Receipt با Anchor عددی، عنوان جزئیات و Schema اختصاصی خرید، فروش، واریز، برداشت، دریافت فیزیکی، سفارش/هشدار، اقساط، هدیه یا پاداش ساخته می‌شود. فیلد غایب حذف و هرگز با نرخ زنده یا مقدار فرضی پر نمی‌شود. قرارداد اجرایی در `receipt-result-structure-contract-v0.1.md` ثبت است.
- `Observed fact` — D-208/V-142 جداسازی Composition در D-207 را جایگزین کرد: Result و Receipt اکنون یک صفحه تطبیقی «جزئیات عملیات» هستند. موفق، ناموفق و Pending در صورت داشتن Record مبلغ/مقدار، زمان، کد پیگیری و فیلدهای اختصاصی را همان‌جا نشان می‌دهند؛ اثر مالی بعد از جزئیات و با تأکید ثانویه است. خطای پیش از ثبت Record کد پیگیری ندارد و تاریخچه همان Component را باز می‌کند — `operation-detail-structure-contract-v0.2.md`.
- `Observed fact` — با D-209/V-143، «اثر عملیات» از موفقیت‌های ساده حذف و فقط در صورت افزودن معنای تازه با Label «وضعیت موجودی» حفظ می‌شود: عدم تغییر، برگشت/جبران، رزرو، قفل، کسر متفاوت از Anchor یا نتیجه نامشخص.
- `Observed fact` — با D-210/V-144، واریز درگاه بدون Preview/تأیید داخلی پس از Loading انتقال مستقیماً به موفق، ناموفق یا نتیجه نامشخص برمی‌گردد. دو State «در حال بررسی/تطبیق» از مسیر درگاه حذف شده‌اند؛ Unknown منع پرداخت تکراری و بازگشت مبلغ کسرشده حداکثر تا ۷۲ ساعت به همان حساب را نشان می‌دهد. برگشت وجه State بعدی تاریخچه و تطبیق فقط مربوط به روش‌های انتقالی است.
- `Observed fact` — با D-213/V-146، KYC error داخل همان فرم و با حفظ Draft/Retry نمایش داده می‌شود؛ Guest از ساختار خانه اصلی استفاده می‌کند؛ هر دو مسیر ساخت/تغییر رمز از `artifacts/password-policy.js` می‌خوانند؛ صفحه تأیید مستقل کارت حذف و خطاهای قابل‌اصلاح زیر ورودی از عدم مالکیت/اختلال مستقل جدا شده‌اند. `bankCardPicker` به‌عنوان انتخاب‌گر مبدأ/مقصد تأییدشده باقی است. پنج مجوز با متن کامل و Placeholder تصویر ساخته شده و Asset رسمی زیر OQ-063 باز است.
- `Business decision` — طبق D-211، تحویل به چت ساختار بایننس پس از Gate هفت‌بخشی `pre-binance-wireframe-readiness-plan-v0.1.md` انجام می‌شود. مرحله فعلی PB-1 ممیزی Current-state و تولید Punch list است؛ P0/P1 مستقل از بیزینس بسته و موارد D-206/OQ-055 جدا تحویل می‌شوند.
- `Observed fact` — مرحله ۴ ممیزی در V-140 کامل شد: واریز Empty state، نقشه دریافت، Share دعوت، Support context رسید، FAQ و نصب PWA قرارداد واقعی دارند؛ گفت‌وگوی زنده بدون اتصال سرویس Disabled است و CTA دانلود قوانین بدون فایل رسمی حذف شده است — D-205.
- `Business decision` — مرحله ۵ Business-gated طبق D-206 تا Review فلوها با Business Owner متوقف است. وایرفریم فعلی دریافت، اقساط، هدیه، سفارش/هشدار، مجوزها و پشتیبانی برای Review حفظ می‌شود، اما داده‌ها و رفتارهای حل‌نشده فقط `Design assumption` هستند و UI نهایی محسوب نمی‌شوند.
- Geometry و hierarchy خانه D-175/D-178/D-180/D-181/D-183/D-191: عرض هر کارت `calc(100% - 84px)` است؛ در عرض‌های ۳۲۰ و ۳۹۰ Card مرجع به‌ترتیب ۲۰۴×۱۴۸ و ۲۷۴×۱۴۸px است و دقیقاً ۷۴px از Card بعدی در Rail دیده می‌شود. Card دارایی کل Surface روشن با ته‌رنگ ۶٪ Primary، Border کمی قوی‌تر و سایه ظریف دارد؛ Mask دایره‌ای موجودی مخفی این Card همان رنگ/وزن عدد آشکار را دارد. CTA خریدوفروش تمام‌عرض، سفید/Outline و ۴۴px باقی می‌ماند. عدد ارزش/گرم با بازه ۱۸–۲۰px، واحد کوچک‌تر و حالت‌های فشرده ۱۰+ و ۱۳+ رقمی از بیرون‌زدگی جلوگیری می‌کند؛ Scroll-snap و رفتار حفظ موقعیت تغییر نکرده‌اند
- Context نرخ D-160/D-170/D-173/D-185/D-188/D-190: `LivePriceTicker` فشرده زیر Header قیمت فروش طلا/نقره/مس را با اسلاید عمودی حلقه‌ای نشان می‌دهد؛ نام فلز، قیمت و واحد یک ردیف واحدند و حرکت ۴۶۰ms فریم‌به‌فریم است. هر Instance موجود هر سه فلز را می‌چرخاند؛ بازار، معامله و جزئیات نرخ اصلی خود را دارند و صفحه‌های حساس مستثنا هستند. Prototype در Load و هر پنج دقیقه نرخ عمومی طلا/نقره ملی‌گلد و مس شمش‌کاپر را از Endpoint محدود می‌گیرد، بازه/Timeout/Partial را کنترل و ارزش دارایی/Total/Donut را همگام می‌کند. عبارت‌های `به‌روز / آخرین نرخ / نمونه` نمایش داده نمی‌شوند و Benchmark `۱۹٫۱ میلیون / ۴۱۴ هزار / ۲٬۵۴۰ تومان` فقط Fallback داخلی است؛ این اتصال نرخ رسمی شمش یا نرخ قابل معامله نیست و OQ-020/OQ-033 باز می‌مانند
- نمایش مستقیم موبایل D-161: همان فایل Prototype در گوشی پنل سناریو، قاب و Statusbar ساختگی را حذف و App Shell را با عرض کامل، `100dvh` و Safe Area نشان می‌دهد؛ Body ثابت است و فقط Main داخلی اسکرول می‌شود. Desktop همچنان پنل Review و قاب ۳۹۰×۸۲۰ را نگه می‌دارد
- `Observed fact` — Adaptive viewport fit در D-186/V-117 روی تمام Renderها و Resizeها فعال است. پس از D-187 و حذف دو Sheet تاریخچه در D-192، در `390×820` تعداد ۳۴/۴۱ صفحه و ۱۴/۱۶ Bottom Sheet بدون Scroll جا می‌شوند؛ هفت صفحه و دو Sheet طولانی Scroll داخلی کنترل‌شده دارند. در `320×568` نیز Scroll افقی و Touch target زیر ۴۴px صفر است. گزارش کامل: `viewport-fit-qa-2026-08-17.md`
- Prototype مکمل تست معامله: `artifacts/trade-mobbin-recommendations-test-wireframe.html` خروجی مستقل و غیرجایگزین برای آزمودن پیشنهادهای تحقیق Mobbin است؛ اصل/کارمزد/خالص، چرخه نرخ، Preview، Pending و Resultهای موفق/ردشده/نامشخص/آفلاین را با داده آزمایشی پوشش می‌دهد. فایل مرجع فعال تغییر نکرده و این Candidate تصمیم محصول یا قاعده مالی تازه محسوب نمی‌شود
- ممیزی Mobbin خانه: `home-mobbin-structure-audit-2026-08-15.md` با مشاهده مستقیم نمونه‌های Revolut، Coinbase، N26 و Fidelity. با D-159 مسیر سه‌ردیفه کنار گذاشته و حالت Cardمحور نامتقارن تأیید شد: طلا Card اصلی، نقره ثانویه و مس از ادامه Carousel قابل کشف است. خروجی فعال `artifacts/home-mobbin-card-priority-candidate-v2.png` است؛ نسخه سه‌ردیفه فقط تاریخچه Review باقی می‌ماند و Gate مرحله تغییر نکرده است
- ممیزی Mobbin حالت بدون دارایی: `home-zero-portfolio-mobbin-pattern-audit-2026-08-18.md` با مشاهده مستقیم N26، Greenlight، Lightyear، Public، Fuse، MoonPay و Revolut. با D-212/V-145، `WF-MM-018` در Prototype مرجع همان Home shell را حفظ و فقط Rail/CTA دارایی را با Activation card کوتاه جایگزین می‌کند؛ کیف تومان، Shortcutها، Campaign غیرتکراری و Navigation باقی می‌مانند. Gate اجرا `Sufficient for owner review` است
- مسیر موازی D-163/D-177: Audit، G4 mapping و G5 Visual Direction Contract v0.1 کامل‌اند و Pilot غیرمخرب Home/Assets روی کپی مستقل اجرا شده است؛ قدم بعد Review مالک محصول و بازآزمایی H05 Dynamic Type 200% است. فایل مرجع و قواعد رفتاری شمش تغییر نمی‌کنند. خدمات و قابلیت‌های Capability-gated فقط Pattern عمومی را تغذیه می‌کنند و از رفتار بایننس Scope یا تصمیم محصول استنتاج نمی‌شود
- `Observed fact` — Pilot غیرمخرب `artifacts/multi-metal-waves-1-and-3-binance-alignment-pilot.html` تنها فایل اجرایی G5 است و فایل مرجع `multi-metal-waves-1-and-3-wireframe.html` حفظ شده است. Home اکنون Canvas سفید، ارزش کل فلزها به‌عنوان تنها Numeric hero، Cardهای Object با ارزش فعلی Primary و وزن/P&L در Meta، CTA واحد، کیف و Shortcut تخت و Campaign کم‌اولویت دارد. Assets/Portfolio نیز Summary تایپوگرافیک، Chart module مستقل و Rowهای ۶۴px بدون Wrapper Card دارد. Entry معامله طبق D-197/V-130 ابتدا از نظر سبک هم‌راستا و سپس طبق D-198/V-132 بر پایه مختصات مرجع `348×743` دقیق شد: تب‌ها، سگمنت `106×38px`، موجودی، مبلغ راست‌چین/واحد چپ، معادل، فضای خالی، CTA `44px`، Keypad `187px` و Nav `68px`. نرخ بصری فقط در Entry این Pilot حذف و در Preview/نام دسترس‌پذیر حفظ شده است؛ D-157 Prototype مرجع تا تصمیم Promote پابرجاست. Geometry فعال D-180 با Cardهای `Rail - 84px` و Peek برابر ۷۴px حفظ شده است. JavaScript Parse، Assertions هندسه و `git diff --check` پاس شدند؛ QA قبلی Home/Assets معتبر است، اما QA بصری تازه Entry معامله به‌علت سیاست امنیتی Browser و H05 Dynamic Type 200% هنوز بازند. `Design assumption` — این خروجی Candidate است و جایگزین Prototype مرجع یا تصمیم نهایی Visual Foundation نیست
- `Observed fact` — نمونه مستقل `artifacts/gold-purchase-success-binance-pattern-pilot.html` برای Review صفحه «خرید موفق طلا» ساخته شده است. الگوی `BN-P-024` را به یک Full-page نتیجه/رسید سبک تبدیل می‌کند: یک Numeric anchor برای وزن، Status صریح، اثر معامله، ردیف‌های تخت نوع/مبلغ/نرخ/روش/زمان/کد پیگیری و اقدام پایین صفحه. اطلاعات مالی تازه یا Row کارمزد اختراع نشده و فایل مرجع و Pilot Home/Assets تغییر نکرده‌اند. `Design assumption` — این خروجی V-129 فقط Candidate قابل Review است و تا تأیید مالک محصول جایگزین Result/Receipt فعال نمی‌شود
- مرجع چیدمان معامله: `artifacts/trade-binance-kraken-layout-candidate.html` با D-157/D-158 در Prototype فعال ادغام شده است: Tab متنی ۱۴px بدون Indicator، سگمنت ۱۱۸×۴۰ خرید/فروش روبه‌روی نرخ، موجودی Contextual، عدد تطبیقی واقعاً مرکزچین، معادل، CTA مستقیم و کیبورد همیشه‌باز. Listbox با Label واحد و آیکن تبدیل یک‌لمسی تومان/گرم جایگزین شده و متن راهنمای ثابت و مقادیر پیشنهادی نمایش داده نمی‌شوند؛ محل پیام زیر ورودی فقط برای Error باقی می‌ماند. داده‌ها نمونه‌اند و Recovery مستقیم کمبود موجودی در OQ-061 باز مانده است
- `Design assumption` — طبق D-196/V-131، سقف رسمی مبلغ و وزن معامله باز است و ظرفیت فعلی Prototype در تومان و گرم فقط ۱۰ رقم است. اندازه پیش‌فرض عدد ۲۱px با رشته پهن ۱۰رقمی «۵» در Viewport عرض ۳۲۰px برای هر دو واحد بدون Scroll/Clipping پاس شده است؛ اعلام سقف رسمی نیازمند تصمیم و تست دوباره خواهد بود
- اصلاح فعال D-135: دریافت فیزیکی هر سه فلز با Entry مشترک، کاتالوگ Multi-select/Stepper، جزئیات تحویل، Sheet اثر مالی و Success/Failed؛ داده‌های SKU/خلوص/کارمزد موقت‌اند و مشخصات در `multi-metal-physical-delivery-flow-spec.md` ثبت شده است
- جریان فعال: Review و اصلاح جزئی موج‌به‌موج توسط مالک محصول؛ QA مرورگری سرتاسری ۳۳ State اصلی قبلاً پاس شده و اصلاح دریافت با Syntax و ۱۸ Assertion محاسبه/ناوبری بازآزمایی شده است

### قانون سراسری اقدام پایین صفحه — D-144 / D-187

- CTA اصلی و گروه اقدام نهایی هر فلو تمام‌صفحه در ردیف مستقل `page-action-root`، خارج از `appMain` اسکرولی و بین Main و Bottom Navigation قرار می‌گیرد؛ بنابراین بدون نیاز به پیمایش همیشه در کف Viewport یا بالای Bottom Navigation دیده می‌شود.
- ردیف اقدام Safe Area را رعایت می‌کند، فضای مستقل در Grid شل دارد و محتوای Main را نمی‌پوشاند.
- اقدام نهایی Bottom Sheet به پایین همان Sheet می‌چسبد.
- Chip، انتخاب‌گر، فیلتر، اقدام Contextual داخل Card، دکمه ناوبری و Dialog غیرتمام‌صفحه در جای محتوایی خود باقی می‌مانند.
- در Prototype فعال، Renderer گروه اقدام نهایی مستقیم صفحه و گروه اقدام حالت فعال اقساط را پس از Render به `page-action-root` منتقل می‌کند. صفحه‌های تازه باید Action group نهایی را `action-stack` مستقل از Card/Row بسازند؛ اقدام Contextual نباید به‌اشتباه در این گروه قرار گیرد.

### قانون سراسری Header — D-140 / D-146 / D-179

- Header سه Slot فیزیکی ثابت `Left / Center / Right` دارد و جهت RTL متن نباید جای آن‌ها را معکوس کند.
- در صفحه‌های Root، پروفایل در چپ، عنوان در مرکز و اعلان در راست است.
- در زیرصفحه‌ها، سمت چپ خالی و دکمه بازگشت با Chevron رو به راست (`>`) در سمت راست است.
- Entry اصلی تب «معامله» استثنای Root است: Header مشترک نمایش داده نمی‌شود، Asset Tab از ابتدای محتوا می‌آید و عنوان صفحه فقط به‌صورت `sr-only` حفظ می‌شود. Preview و زیرصفحه‌های معامله همچنان Header بازگشت دارند.
- Prototype مرجع باید این قرارداد را با Grid Areaهای صریح پیاده کند؛ عنوان همچنان RTL باقی می‌ماند.

### قانون سراسری پلتفرم و تعامل — D-151 / D-152 / D-154 / D-157 / D-158

- Baseline همه وایرفریم‌ها و Prototypeها گوشی، لمس و PWA است؛ Android همان WebView این تجربه باقی می‌ماند.
- کنترل لمسی حداقل ۴۴–۴۸px، ورودی متنی/عددی حداقل ۱۶px، `inputmode` مناسب مبلغ/وزن/OTP، Safe Area و حفظ Field/CTA هنگام بازشدن کیبورد الزامی‌اند.
- هیچ اطلاعات، اقدام یا Recovery ضروری فقط با Hover آشکار یا قابل اجرا نیست؛ Hover فقط در `@media (hover:hover) and (pointer:fine)` بازخورد یا میان‌بر وب است.
- وب عریض از همان ساختار معنایی با Breakpoint پاسخ‌گو، Keyboard/Focus کامل و چگالی متناسب استفاده می‌کند؛ مسیر محصول یا Component موازی برای Desktop ساخته نمی‌شود.
- انتخاب سه فلز معامله Asset Tab مستقیم است؛ کاتالوگ بزرگ‌تر در موبایل Bottom Sheet و در وب عریض Popover/Dialog خواهد بود.
- Entry معامله در Portrait استاندارد بدون اسکرول است: Tab متنی فلز بدون Indicator، نرخ منتخب کنار خرید/فروش فشرده، موجودی Contextual، عدد مرکزچین، تغییر مستقیم تومان/گرم با آیکن کنار واحد، معادل، CTA نام فلز و کیبورد همیشه‌باز با ترتیب `۱..۹ / ممیز ۰ حذف`. متن عادی و Preset زیر ورودی وجود ندارد و همان محل فقط Error مقدار را نشان می‌دهد. Preview/Result همچنان قرارداد CTA پایین D-144 را حفظ می‌کنند.
- QA پیش از ارائه باید حداقل Touch موبایل، بازشدن Keyboard، Mouse/Hover شرطی، Keyboard/Focus و عرض‌های ۳۲۰/۳۷۵/۳۹۰/Desktop را جداگانه بررسی کند.

## جریان فعال موج ۲

- دامنه بازتنظیم‌شده: واریز ریالی و اتصال آن به کیف/تاریخچه موجود؛ خرید/فروش/رسید معامله از موج ۱ reuse می‌شوند — D-103.
- بسته آمادگی: `wave-2-deposit-flow-readiness-pack.md`
- Prototype فعال: `artifacts/wave-2-deposit-wireframe.html`
- وضعیت Gate مسیر عادی: `Sufficient to prototype`
- مسیر بیشتر از ۴۰۰ میلیون: Concept وابسته تا رفع تعارض OQ-031؛ قانون UI نهایی نیست.
- وضعیت ساخت: مسیر مبلغ، پنج روش، Gate نقش‌ها، تحویل/ثبت، چهار نتیجه، رسید و اتصال کمبود خرید ساخته و در ۳۲۰/۳۹۰px QA شده‌اند.
- قدم بعدی وایرفریم: Review مالک محصول روی Prototype واریز؛ سپس اصلاح یا بستن موج ۲.

## جریان فعال موج ۳

- دامنه: برداشت ریالی از مبلغ و مقصد تا Preview، status، رسید، برگشت و پشتیبانی مرتبط — D-104.
- بسته آمادگی: `wave-3-withdrawal-flow-readiness-pack.md`
- وضعیت Gate مسیر عادی: `Sufficient to prototype`
- reuse: Card کیف و صفحه مستقل تاریخچه موج ۱؛ الگوهای رسید/پشتیبانی به‌صورت نوع‌محور تکمیل می‌شوند.
- خارج از Scope: انتقال طلا طبق D-087، پنل عملیات، حذف/ویرایش مقصد تا OQ-053 و کپی حقوقی hold تا OQ-044.
- قدم بعدی وایرفریم: ساخت فایل مستقل برداشت از مبلغ/مقصد تا Preview، status و نتیجه و انجام QA موبایل.

## روش اجباری پیش از ساخت

1. برای هر فلو یک نسخه از `flow-readiness-pack-template.md` تکمیل شود.
2. قواعد قطعی، فرض‌ها، سؤال‌های باز و ریسک‌ها با منبع از هم جدا شوند.
3. eligibility، تمام ورود/خروج‌ها، قرارداد Field/Component و حالت‌های خطا/بازیابی ممیزی شوند.
4. طراح/دستیار پیشنهادهای پیش‌دستانه و مواردی را که مالک محصول مطرح نکرده ارائه کند.
5. فقط وضعیت `Sufficient to prototype` مجوز ساخت یا بازسازی اساسی است.
6. پس از ساخت، QA تعاملی، بصری، Responsive و دسترس‌پذیری پیش از ارائه به مالک محصول انجام شود.
